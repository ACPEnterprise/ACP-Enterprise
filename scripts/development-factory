#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
export PYTHONPATH="${repo_root}/backend${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m development_factory --repo-root "$repo_root" "$@"
