import { Pressable, StyleSheet, Text } from "react-native";
import { colors, spacing } from "../design/tokens";
export function PrimaryButton({ label, accessibilityLabel, disabled = false, onPress }: { label: string; accessibilityLabel?: string; disabled?: boolean; onPress(): void }) { return <Pressable accessibilityRole="button" accessibilityLabel={accessibilityLabel ?? label} accessibilityState={{ disabled }} disabled={disabled} onPress={onPress} style={[styles.button, disabled && styles.disabled]}><Text style={styles.text}>{label}</Text></Pressable>; }
const styles = StyleSheet.create({ button: { minHeight: 56, backgroundColor: colors.brand, borderRadius: 10, paddingHorizontal: spacing.md, justifyContent: "center", alignItems: "center" }, disabled: { opacity: 0.5 }, text: { color: colors.surface, fontSize: 17, fontWeight: "700" } });
