# HCP.MIGRATION.1A final gate

This milestone binds owner choices without persisting HCP migration candidates.
Protected receipts are stored with the HCP.MIGRATION.1A evidence package; raw
provider evidence remains outside Git.

## Binding integrity

`OwnerDecisionBinding` requires the exact reviewed group digest and a defined
alternative. `OwnerDecisionRecordBinding` additionally requires one unique
native identity for every member of a mixed group. This permits six human HCP
identities to become future Employee candidates while retaining the non-human
marketing identity as excluded source evidence. No Employee row is created by
binding.

## Isolated tenancy prerequisite

The platform bootstrap is intentionally not used because it also creates an
administrator, credential, membership, roles, and permissions. The migration
rehearsal scope initializer uses the existing Company and Branch models and the
platform bootstrap advisory lock, rejects non-loopback/non-rehearsal targets,
and fails unless both tenancy tables are empty. It creates only one active
Company and its active primary Branch. HCP candidate tables remain empty.

The initialized isolated target identity is:

- Company `3ddf07ce-0f44-4b67-a40f-fb0ec41bb7cd` (`ACP`)
- primary Branch `887f413a-70dc-4ab1-98aa-8e84f4e7efd0` (`PLUMBING`)

These UUIDs were generated by the canonical Enterprise models in the isolated
database; they were not manually invented or copied from another environment.

## Estimate continuity correction

The bounded 89-record review found an authoritative acquired-source seam that
the first packet did not use: `Job.original_estimate_id` and
`Job.original_estimate_uuids` reference Estimate option native IDs.

- 22 Estimates link deterministically to open Jobs.
- 42 link deterministically to non-open Jobs.
- 1 unlinked Estimate has terminal historical status.
- 24 remain current or ambiguous and have no authoritative Job link.

Customer or Location overlap alone is never treated as a relationship. The 64
native-reference relationships are a transformation/reconciliation addendum;
SOURCE.4 is not rewritten. The 22 open-linked Estimates must accompany their
Jobs for continuity. The 24 residual records require an explicit owner choice
or a target contract that supports unlinked Estimate exceptions.

## Remaining gates

Branch scope still requires the owner to bind both the HCP `Plumbing` Business
Unit assertion and the no-Business-Unit default to the isolated primary Branch.
The 24 residual Estimate dispositions also remain unbound. HCP.MIGRATION.2 must
not start until those decisions are sealed.
