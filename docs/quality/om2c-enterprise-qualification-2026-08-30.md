# OM2-C Enterprise qualification evidence — 2026-08-30

## Authority and scope

- Starting protected authority: `72f969bac74a0bf4c741bdc98133b5c23f18d91e` (`origin/customer-management-v1`).
- Reconciled protected authority: `1fac637462b01869cc7962d7463af1dc12637a43`.
- Latest reconciled protected authority: `a976fa1a18246f546140faf288e094cd10f9ddd0`.
- Qualification branch: `work/om2c-quality-security-program-1`.
- The supplied OM2-A and OM2-B SHAs are not ancestors of starting authority. Their named remote branches were absent after `git fetch --prune`; no integration claim is made.
- This is non-production evidence. It is not Production release approval.

## Risk-weighted coverage map

| Domain | Current evidence | Highest-priority gap / limitation |
| --- | --- | --- |
| Platform / Auth / AuthZ | Strong unit, API, persistence, isolation, idempotency, provider reliability, security-output, header and permission-matrix suites (23 files) | Redis-backed authentication qualification requires an integrated Redis runtime; broad DB fixtures are not self-cleaning |
| Customers | Strong service/API/search/lifecycle/normalization coverage (10 files) | Cross-domain Customer→Location→Job object-binding rehearsal remains integration-level |
| Estimates | Pricing, workflow, conversion, normalized conversion replay, exactly-one Job/event replay, and list API coverage (5 files) | Deterministic multi-connection acceptance/conversion stress is blocked by the transaction-bound shared-connection fixture and remains a dedicated-suite gap |
| Jobs | API, permission, persistence, query, lifecycle service, deterministic lifecycle winner/event, and safe recovery-envelope coverage (5 files) | Broader cross-workflow Job/Appointment/Dispatch competing-transition stress remains incomplete |
| Scheduling / Dispatch | API, persistence, service, assignment serialization, digest-bound replay, contradiction rejection, and reschedule coverage | Large projection performance baselines and true multi-connection reschedule stress remain limited |
| Field Service | Contract, conformance and migration coverage (3 files) | Provider/storage failure composition is integration-only |
| Inventory / Purchasing | Foundation, adjustments, reservations, bounded cycle-count history, purchasing policy, replenishment, three-way match, current-authority replay enforcement, and deterministic multi-session concurrent replay coverage for movements, reservations, adjustments, cycle counts, PO receipts, Inventory application, and Procurement Match/Event authority | Purchasing workspace composes unbounded vendors/orders/requisitions/policies/documents and performs at least nine queries per PO plus receipt/return queries; `PENDING_OM2A_BATCHED_WORKSPACE_READ_MODEL` because correcting the payload/read-model semantics belongs to Supply Chain |
| Price Book | API and service coverage (2 files) | Catalog composes every service item/version/component/option into one response; safe pagination requires coordinated owning-product read-model/UI semantics and remains `PENDING_PRICE_BOOK_PAGED_READ_MODEL` rather than silently truncating authority |
| Invoicing / Payments / AP | Contract, provider, AR/AP invariant, recovery-envelope, contradictory replay, and multi-connection collection/refund/disbursement-application exactly-once coverage | Deposit/settlement concurrency and cross-domain double-count rehearsal need broader end-to-end evidence |
| Accounting | Posting, API, contract, migration, balanced persistence, concurrent create/post/reversal, exactly-one Event, and safe recovery coverage | Full synthetic AR/AP/Payroll settlement balance rehearsal remains integration-level |
| Payroll | Strong policy, calculation, approval/finalization, payment, remittance, reporting and protected artifact coverage (23 files) | Redis/storage integrated failure injection and payment-provider restart replay remain pending |
| Migration | Extensive Customer and operational migration evidence (46 files) | Protected external source/storage rehearsal remains environment-dependent; no real provider contacted |
| Business Economics | Policy, admission, allocation, profitability, source-conformance, owner-workspace and explicit Company/Branch persistence enforcement | Cross-source economic double-counting still needs a consolidated rehearsal |
| Beacon | Evaluation, lifecycle, workflow, prioritization and acknowledgement coverage (16 files) | Read-only versus operational mutation matrix should be expanded at API level |
| LIA | Governed deterministic retrieval, permission-selected adapters, tenant/Branch-scoped counts, injection/exfiltration refusal, non-executing proposals, mutation classification, and frontend recovery coverage | Durable transcript/feedback retention remains `BLOCKED_POLICY`; no external AI provider is configured or contacted |
| Mobile-facing APIs | Backend employee/engineering APIs and separate mobile Jest suite exist | Native mobile was not changed in this tranche; integrated device accessibility remains external |
| Administration / frontend | Route/component tests cover authorization and representative loading/error states | Automated accessibility tooling and responsive viewport regression are not comprehensive |

## Qualification results

- Frontend after latest authority reconciliation and security repairs: 101 test files / 326 tests passed; repository-wide ESLint passed; TypeScript and production Vite build passed.
- Backend focused security tranche: isolation, authorization composition, idempotency, provider uncertainty and safe-output tests passed after repairing a stale catalog fingerprint.
- Backend latest broad run on fresh isolated r21 authority: 2,264 passed, 7 skipped, 1 failed. The sole failure is `ENVIRONMENT`: the configured Docker hostname `redis` has no local runtime and authentication rate limiting fails closed with `RateLimitUnavailableError`. No local Redis binary or running Docker engine is available, so this remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`; no functional regression was observed.
- Static: Python 3.12 compilation passed; MyPy passed across 621 source files. Current repository application Ruff remains an explicit non-clean gate with 25 pre-existing findings, predominantly import ordering; the two files changed by the latest reconciliation repair pass focused Ruff, and no mass formatting was performed.
- Database: PostgreSQL 16.15 fresh zero-to-head upgrade passed again on isolated database `acp_om2c_r27`; exactly one candidate Alembic head `n2l1j60i7g3e`; `current=head`; `alembic check` reports no drift. The immutable Economics result-history revision `h6f8j0l2n497` and shared event/audit scope revision passed a combined downgrade to `l0j9h48g5e1c` and re-upgrade; forward Economics Branch-lineage revision n2 also passed focused downgrade/re-upgrade, while earlier Luminary and revision-collision qualification remains preserved.
- Secret scan: only intentional synthetic canary tests matched the high-confidence token/private-key patterns; no discovered secret value was emitted.
- Non-production performance baseline on the local OM2-C host: 200 warmed in-process ASGI requests to `/health/live` produced 0.543 ms median, 0.746 ms p95, and 94.145 ms maximum; the isolated maximum is retained as observed local scheduling noise rather than discarded. A representative r18 pytest duration run across Customer, Job, Scheduling, Purchasing, Payroll reporting, and Migration API/projection tests passed 90/90 in 14.96 seconds. Slowest call phases were Customer replay/scope 0.59 s, Scheduling reschedule-conflict 0.28 s, Scheduling scoped query/detail 0.21 s, and Customer tenant workflow 0.18 s; Purchasing, Payroll reporting, and Migration calls did not enter the top-25 cutoff of 0.11 s. These values include local test fixtures and in-process transport, are only gross-regression baselines, and are not Production latency SLOs or load-test evidence.

## Defects and repairs

1. `STALE_FIXTURE`: sensitive-output catalog expansion changed the deterministic fingerprint without updating its qualification assertion. The assertion now binds the expanded catalog.
2. `STALE_FIXTURE`: external-adoption successor evidence did not satisfy newly integrated execution-head and repository-readiness admission. The test now supplies matching authoritative-head evidence and isolates the orthogonal readiness adapter with an explicit mock.
3. `DEPENDENCY`: direct `react-router` 7.18.1 fell within the lockfile audit's affected CSRF-bypass range. It was advanced only to the patched 7.18.3 line. Remaining audit findings are transitive and require separate applicability/upgrade qualification.
4. `AUTHORIZATION/DURABLE_INTEGRITY`: Economics execution checked Company identity but accepted a result tagged to a different or nonexistent Branch, and the durable result table lacked a Company+Branch foreign key. Persistence now rejects active-branch escape and nonexistent Company branches before mutation; migration `c1a0e38d6bfc` enforces the same invariant in PostgreSQL. All 140 Business Economics tests pass, and the new migration passed fresh upgrade, downgrade/re-upgrade, current-head, and drift qualification.
5. `FRONTEND_AUTHORIZATION/RECOVERY`: the Economics route initiated its protected workspace request even when the current user lacked read authority, and its temporary-unavailable state had no recovery action. The query is now disabled unless read permission is present, and the safe error state exposes a tested retry without rendering backend details.
6. `OBJECT_AUTHORIZATION/IDEMPOTENCY`: Purchasing resolved PO-scoped durable receipts before checking the caller's current Branch authority. Possession of an exact key and request could therefore replay a Branch A PO mutation result to a same-Company caller restricted to Branch B. The shared Purchasing replay boundary now reauthorizes the parent PO before receipt lookup for update, line, lifecycle, receipt, discrepancy, return, disposition, and change operations. The adversarial cross-branch replay fails closed, and 76 Purchasing/Procurement/Inventory tests pass.
7. `CONCURRENCY/IDEMPOTENCY`: Inventory movement, reservation, adjustment, cycle-session and cycle-entry creation used read-before-insert replay checks without serializing same-key transactions. Concurrent exact replay could expose raw unique-constraint failures, and adjustment replay could create a movement before losing the adjustment race. Each Company+operation+normalized-key now takes a transaction-scoped PostgreSQL advisory lock before authoritative lookup. Deterministic two-transaction tests prove one returned identity and one durable record; all 28 Inventory tests, focused Ruff, MyPy, and diff checks pass.
8. `CONCURRENCY/IDEMPOTENCY`: The shared Purchasing replay boundary performed a read-before-insert lookup without serializing Company+key. PO mutations happened to gain protection from parent-row locks, but create-style commands could race into the command-receipt unique constraint; the Vendor test labeled concurrent only exercised sequential replay. The shared boundary now takes a transaction-scoped PostgreSQL advisory lock after any required current-authority check and before receipt lookup. True concurrent Vendor replay returns one authority, competing receipt commands retain one typed winner, and 80 Purchasing/procurement-matching/Inventory tests pass.
9. `IDEMPOTENCY`: Estimate conversion validated and persisted a trimmed idempotency key but compared replay evidence to the untrimmed request. A whitespace-bearing key therefore succeeded once and falsely conflicted on exact replay. Conversion now normalizes once before validation, comparison, and persistence; replay produces one Job, one conversion, and one Business Event. All 32 Estimate tests pass. The existing Estimate fixture binds sessions to one transaction-scoped AsyncConnection, so it cannot truthfully execute concurrent driver operations; multi-connection stress remains an explicit qualification gap.
10. `API_RECOVERY_CONTRACT`: Job lifecycle state, stale-version, validation, and concealed-not-found failures returned safe but unclassified string details, leaving clients unable to distinguish refresh from correction or terminal recovery. The boundary now uses the standard safe failure envelope: stale/state conflicts are `RETRY_AFTER_REFRESH`, validation is `USER_CORRECTION_REQUIRED`, and concealed not-found is `TERMINAL_FAILURE`, with correlation identity when available. A deterministic two-transaction activation race proves one authoritative state/version/Event and one typed stale loser. All 190 Jobs tests, focused Ruff, MyPy, and diff checks pass.
11. `IDEMPOTENCY/DURABLE_EVIDENCE`: Dispatch history bound replay only to Company, key, Appointment, and event type. The same key and operation with a different technician, reason, crew member, version, exception, arrival state, or reconciliation choice could therefore be accepted as replay. New Dispatch command evidence stores a canonical SHA-256 request digest protected by a database length constraint, and every replay compares it before returning authority. Legacy rows without sufficient request evidence fail closed. Migration `d2b1f49e7c0a` passed downgrade/re-upgrade, current-head, and drift checks; all 61 Scheduling/Dispatch tests plus focused Ruff and MyPy pass.
12. `API_RECOVERY_CONTRACT/SAFE_OUTPUT`: Scheduling and Dispatch translated controlled domain errors into bare string details without the standard recovery vocabulary; Dispatch also reflected internal domain exception messages directly. Both boundaries now return correlation-capable `SafeFailure` envelopes. Stale version, capacity, and state conflicts use `RETRY_AFTER_REFRESH`; validation uses `USER_CORRECTION_REQUIRED`; concealed not-found uses `TERMINAL_FAILURE`. Tests assert safe codes and ensure Dispatch internal details are not reflected. All 67 Scheduling/Dispatch tests plus focused Ruff, MyPy, and diff checks pass.
13. `CONCURRENCY/ECONOMIC_IDEMPOTENCY`: Payment collection locked a matching intent row, which cannot serialize concurrent creators when no row exists. Refund and deposit checked replay before later receipt locks without rechecking, allowing concurrent exact commands to race into unique constraints. Webhook, settlement, and posting receipt creators had the same absent-row pattern. A shared Company+operation+identity PostgreSQL transaction advisory lock now protects these creation boundaries before replay lookup. A new independent-engine test proves concurrent collection/refund replay creates one intent, one receipt, one refund, one balance mutation, and one intent Event, while contradictory replay fails closed. All 18 Payments/Invoicing tests plus focused Ruff, MyPy, and diff checks pass.
14. `PROVIDER_UNCERTAINTY`: Synthetic possible-acceptance ambiguity is qualified explicitly. An ambiguous collection persists the intent as `reconciliation_required`, creates one reconciliation exception, creates no payment receipt, and exact replay returns that authority without invoking the provider again. Provider call counting also proves concurrent captured replay invokes collection once. All 19 Payments/Invoicing tests pass; no real provider was contacted.
15. `API_RECOVERY_CONTRACT/SAFE_OUTPUT`: The Payments router reflected controlled exception strings directly, including Payment conflicts wrapping downstream Invoice messages, and returned no recovery classification. Payment not-found, conflict, and validation responses now use fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED` respectively. Branch concealment uses the same non-reflective not-found contract. Adversarial canary details are absent from responses; all 22 Payments/Invoicing tests plus focused Ruff, MyPy, and diff checks pass.
16. `TEST_ISOLATION/STALE_FIXTURE`: Protected authority `4c00ac6` introduced independent owner-read acceptance changes. Cold selective Platform runs exposed model-registration dependencies masked by broad-suite import order, and the catalog-sync test still assumed Scheduling manage was assigned to the bootstrap administrator despite the new read-only policy. Bootstrap and Launch Controls tests now import their referenced Customer/Inventory/Scheduling model graphs explicitly, and the sync fixture expects no manage assignment before deleting the catalog permission. Independent cold processes pass: 7 Bootstrap tests and 12 Launch Controls tests. The reconciled Economics persistence boundary retains both current authorized-Branch admission and OM2-C durable Company+Branch validation; 17 focused Economics acceptance tests pass.
17. `ACCOUNTING_CONCURRENCY/SAFE_OUTPUT`: Accounting Core journal creation checked absent client/source replay evidence before later period locking, allowing concurrent creators to race into unique constraints. Reversal creation did not replay its client key and could attempt multiple prepared reversals before one was posted. Ordered Company-scoped advisory identities now serialize client and source authority; reversal returns only matching evidence and rejects contradictions. A new independent-engine persistence suite proves one journal/source, balanced debit/credit truth, one concurrent posting winner and typed stale loser, one posting Event, and one exact-replay reversal. The Accounting API now uses fixed recovery-classified envelopes instead of reflecting exception text. All 43 Accounting/contract tests plus focused Ruff, MyPy, and diff checks pass.
18. `AP_CONCURRENCY/ECONOMIC_IDEMPOTENCY/SAFE_OUTPUT`: AP creation and application boundaries used absent-row replay checks without a shared identity lock. Concurrent exact requests could race into unique constraints, while credit/disbursement source identities lacked safe replay and application keys returned prior authority without rejecting a changed amount or target. Company-scoped advisory identities now serialize Vendor, source, Bill-number, credit, disbursement, application, and posting-receipt authority; replays validate immutable request evidence and contradictions fail as typed AP conflicts. An independent-engine test proves two concurrent 60.00 disbursement applications create one application/subledger fact and reduce both available/open balances exactly once, while changed-amount key reuse fails closed. AP responses now use fixed recovery-classified envelopes without reflected exception text. All 13 AP/procurement-matching tests plus focused Ruff, MyPy, and diff checks pass.
19. `AP_OBJECT_AUTHORIZATION`: AP creation and aging enforced authorized Branch scope, but Bill lifecycle, duplicate override, credit/disbursement application and reversal accepted any valid same-Company resource ID. A Branch-restricted permission holder could therefore mutate a Bill or replay application authority from another Branch. Every public resource mutation now binds the parent Bill/disbursement to the caller's current authorized Branch set before replay or state mutation and conceals unauthorized identities as not found. The adversarial replay test proves an exact valid application key cannot escape its Branch. All 13 AP/procurement-matching tests plus focused Ruff, MyPy, and diff checks pass.
20. `AP_ACCOUNTING_RECEIPT_INTEGRITY`: AP Accounting posting receipt replay compared only status and journal identity. Reuse of the same source Event ID with a different AP source, mapping version, effective date, or failure reason was accepted as replay, weakening source provenance. Replay now binds the complete immutable receipt authority. Independent concurrent exact replay returns one receipt, while changed source identity fails as a typed conflict; all 14 AP/procurement-matching tests pass.
21. `MIGRATION_GRAPH/RELEASE_BLOCKER`: Protected authority `f6cffe4` integrated Supply Chain migrations from `b0ff279c5aeb` alongside OM2-C's Economics/Dispatch lineage from the same parent, producing heads `19i4k89084j8` and `d2b1f49e7c0a`. Repository-standard `alembic upgrade head` failed immediately. Forward-only merge revision `e3c2a71f8d4b` preserves both histories and restores exactly one head. Existing-schema and entirely fresh PostgreSQL upgrades both reach current=head with no drift; all 65 Purchasing/procurement/idempotency tests pass on the reconciled schema.
22. `PROCUREMENT_MATCH_OBJECT_AUTHORIZATION/STALE_APPROVAL_RACE`: Three-way-match replay returned authority before reauthorizing the referenced PO/Bill and compared only their IDs, so a valid key could escape Branch scope or accept changed expected versions. AP approval also read match freshness without locking the PO, allowing a receipt or other PO mutation to commit immediately after validation and leave an approved Bill backed by stale evidence; its active-match lookup did not exclude superseded history. Replay now reauthorizes both parents and binds expected versions. AP approval shares the per-Bill match advisory identity, selects only unsuperseded authority, and locks the PO while recomputing the complete source digest. A deterministic competing-writer test proves freshness waits and then rejects changed PO authority. All 65 Purchasing/Matching/AP tests plus focused Ruff, MyPy, and diff checks pass.
23. `PURCHASING_DOCUMENT_CONCURRENCY`: Newly integrated document custody uniquely constrained Company+entity+content but serialized only by command key. Concurrent registration of the same artifact under different keys could therefore expose a raw uniqueness failure. Content identity now receives its own transaction advisory lock. Exact normalized custody metadata converges on one immutable record while a changed filename, media type, storage reference, source reference, document type, or Branch for the same content fails as a typed conflict; each accepted alternate key receives durable replay evidence without duplicating the registration Event. The focused backend suite passes 49 tests, the Purchasing route passes 4 frontend tests, and changed-file Ruff, Purchasing MyPy, and diff checks are clean. Two unrelated newly integrated BANK test files retain pre-existing Ruff import-order findings and were not mass-formatted.
24. `PURCHASING_API_RECOVERY/SAFE_OUTPUT`: The expanded Purchasing router reflected controlled domain exception strings directly and returned no machine-readable recovery classification. Inventory/provider-adjacent wrapped errors could therefore expose internal details through receipt and return endpoints. Not-found, conflict, validation, and fallback Purchasing failures now use fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, `USER_CORRECTION_REQUIRED`, and `OWNER_ADMIN_ACTION_REQUIRED` respectively. Adversarial canary details are absent from every response class. The focused backend suite passes 50 tests, the Purchasing frontend route passes 4 tests, and the supported TypeScript plus production Vite build passes.
25. `SUPPLY_CHAIN_BUSINESS_IDENTITY_CONCURRENCY`: Requisition number, first policy per Company+Branch+type, and PO number created during requisition conversion were database-unique but serialized only by command key or parent row. Different-key concurrent creators could race into raw uniqueness failures. Each business identity now takes a transaction advisory lock before its absence check; competing authority fails as a typed Purchasing conflict. Independent connections prove exactly one requisition and one policy survive their respective races, and conversion applies the same protection to PO numbers. The focused backend suite passes 51 tests with clean changed-file Ruff, Purchasing MyPy, and diff checks.
26. `REQUISITION_OBJECT_AUTHORIZATION`: Requisition transition replay validated only Company ownership before returning prior submit/approve/reject/convert/cancel authority. A caller restricted to another Branch could retrieve the transitioned requisition using its valid identity and exact command key. Both create and lifecycle replay now reauthorize the current requisition Branch and conceal inaccessible authority as not found. A direct cross-Branch submit replay fails closed; the focused backend suite remains 51/51 with clean changed-file Ruff, Purchasing MyPy, and diff checks.
27. `FRONTEND_FAILURE/ACCESSIBILITY`: Purchasing document custody discarded the promise from an async mutation handler. A rejected registration could surface as an unhandled promise rejection; the passive failure banner was not announced and offered no explicit retry. The component now contains mutation rejection, retains all entered evidence, emits an assertive accessible alert without backend details, and provides a retry using the retained input. Fields clear only after confirmed success. Component plus route tests pass 5/5, focused ESLint passes, and the supported TypeScript plus production Vite build passes.
28. `PROTECTED_STORAGE_REFERENCE_DISCLOSURE`: Purchasing document custody correctly kept storage locators out of Business Events, but the general workspace and registration response serialized the raw `storage_reference` to every Purchasing read/manage caller. No frontend consumer used it, so this unnecessarily exposed internal provider/bucket/path or signed-reference material. Custody persistence and contradiction comparison retain the locator server-side, while the public document item omits it and continues exposing non-sensitive source provenance plus content/evidence digests. The focused backend suite passes 41 tests; frontend component/route tests pass 5/5; focused Ruff, MyPy, ESLint, TypeScript, and production build gates pass.
29. `REQUISITION_NESTED_OBJECT_AUTHORIZATION/DURABLE_INTEGRITY`: Requisition `job_id` was accepted and persisted without resolving the Job, and the table had no foreign key. Arbitrary, cross-Company, or cross-Branch Job identities could therefore become durable demand provenance. Creation now admits only an existing Job matching the requisition Company and Branch. Forward migration `f4d3b82a9e5c` enforces the same composite relationship against Jobs' Company+Branch+ID authority. Purchasing plus the full Jobs suite passes 230 tests; the migration passes upgrade, downgrade/re-upgrade, current=head, single-head, and drift qualification; focused Ruff, MyPy, and diff checks pass.
30. `INVOICING_CONCURRENCY/REPLAY/SAFE_OUTPUT`: Invoice creation checked replay before locking its Estimate but not afterward. Concurrent same-key creation, or different-key creation for one Estimate revision, could race into idempotency or revision uniqueness constraints. Payment receipt registration similarly performed an absent-row lookup without a shared lock. Creation now rechecks after the Estimate lock and converges exact alternate-key authority on one Invoice while rejecting contradictory commercial terms; payment receipts serialize by Company+receipt identity. Receipt and Accounting posting replays bind their complete immutable evidence rather than only digest or journal/status subsets. The Invoicing router now returns fixed correlation-capable recovery envelopes without reflecting controlled exception text. Independent connections prove one payment receipt and contradictory amount/digest rejection. Invoicing/Payments/Accounting composition passes 67 tests with clean focused Ruff, MyPy, and diff gates. Four existing shared-connection fixture rollback warnings remain classified `TEST_ISOLATION`.
31. `PROCUREMENT_MATCHING_API_RECOVERY/FRONTEND_FAILURE`: Procurement Matching reflected controlled exception strings directly and returned no standardized recovery classification. The workbench did not render those strings, but rejected evaluation and exception-resolution promises escaped their event handlers, and provider-like reconciliation or temporary-unavailable classifications had no explicit safe presentation. Matching now returns fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED`; an adversarial canary proves internal details are absent. The workbench contains rejected mutations while retaining entered evidence and resolution notes, recognizes structured reconciliation and availability states without rendering backend messages, and announces failures assertively. Procurement Matching/AP/Purchasing composition passes 68 tests; the component passes 3 tests; full ESLint, TypeScript, production Vite build, focused Ruff, and diff gates pass.
32. `INVENTORY_API_RECOVERY/FRONTEND_FAILURE`: Inventory not-found, conflict, and validation exceptions were reflected directly from quantity and reservation operations with no standardized recovery classification. Create, transfer, reserve, allocate, and release UI handlers also allowed rejected mutation promises to escape; create forms cleared only after success but offered only one generic passive message. Inventory now returns fixed correlation-capable `SafeFailure` envelopes using `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED`; a secret-bearing canary is absent from every response. The workspace contains all rejected mutations, retains location/transfer/reservation evidence until confirmed success, interprets structured correction, reconciliation, temporary-unavailable, and terminal states without rendering backend messages, and announces the governed failure assertively. All 29 Inventory backend tests and 6 Inventory route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
33. `PRICE_BOOK_API_RECOVERY/FRONTEND_FAILURE`: Price Book not-found, conflict, and validation exceptions reflected internal text at a commercial pricing boundary, while its fallback lacked machine-readable recovery. All seven UI mutation paths discarded rejected promises and exposed no mutation failure state. Price Book now returns fixed correlation-capable `SafeFailure` envelopes for terminal, refresh, correction, and owner-administrator recovery; canary-bearing internal text is absent from all four response classes. One governed UI wrapper contains category, tax, item, version, activation, option-group, and option failures; clears entered commercial evidence only after confirmed success; and assertively announces structured recovery without backend messages. All 17 Price Book backend tests and 6 route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
34. `ESTIMATE_API_RECOVERY/FRONTEND_FAILURE`: Estimate not-found, lifecycle conflict, validation, and fallback exceptions crossed the Customer→Estimate→Job boundary as reflected or unclassified text. The create-proposal handler allowed rejected promises to escape, and decision controls offered only a passive generic failure state. Estimates now return fixed correlation-capable recovery envelopes for terminal, refresh, correction, and owner-administrator action; adversarial internal text is absent from all four classes. Create rejection is contained while preserving immutable-snapshot proposal inputs. Customer transition/acceptance/rejection controls retain entered evidence and assertively distinguish stale authority from correctable evidence without rendering backend messages. All 33 Estimate backend tests and 7 focused route/control tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
35. `FIELD_SERVICE_API_RECOVERY/MOBILE_FAILURE`: Technician Field Service reflected not-found, lifecycle conflict, and validation exception text through itinerary, Job state, work-note, Customer-disposition, non-billable, and invoice-handoff endpoints. The mobile-facing field panel avoided local success assumptions but collapsed all mutation failures into one passive refresh instruction. Field Service now returns fixed correlation-capable terminal, refresh, and correction envelopes with canary-bearing internal text excluded. The technician panel assertively distinguishes stale authority, correctable retained evidence, reconciliation requiring office review, temporary unavailability, and terminal resource loss without rendering backend messages or enabling blind retry. The supported backend-directory run passes all 10 Field Service contract/conformance/migration tests; the technician recovery test, focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates pass. Two initial root-directory migration-test failures were invocation-path `ENVIRONMENT` results and passed unchanged under the repository-supported backend working directory.
36. `COMMUNICATIONS_AUTHORIZATION_PRIVACY/SAFE_OUTPUT`: Communications reflected authorization, source-not-found, replay-conflict, and validation exception text. Authorization details could distinguish Branch denial, consent policy, or source-domain state, while validation could expose recipient/provider-adjacent data. The boundary now emits fixed correlation-capable envelopes: policy/consent denial and concealed source identity are terminal, contradictory replay requires refresh, invalid input requires correction, and unknown controlled failure requires owner-administrator action. A recipient/provider canary is absent from every response class. All 20 Communications consent, Company/Branch scope, source binding, replay, persistence, permission-composition, and API tests pass with clean focused Ruff, MyPy, and diff gates.
37. `BEACON_AUTHORIZATION_COMPOSITION/SAFE_OUTPUT/TEST_ISOLATION`: Beacon lifecycle and workflow endpoints reflected signal, stale-evidence, ownership-conflict, and invalid-owner details. Claim, assign, transfer, and release were also declared as Analytics-read dependencies even though service policy requires `OWN`, `ASSIGN`, or either for release; valid operators without Analytics read were rejected early, while read-only users reached a later mutation denial. Beacon now emits fixed correlation-capable terminal, refresh, and correction envelopes with signal/provider canaries excluded. Routes bind claim to `OWN`, assign/transfer to `ASSIGN`, and release to a new logged any-of permission dependency; Analytics read alone is denied before service mutation. Direct API tests prove the complete read/own/assign matrix. Collection with Platform authorization exposed a mapper registration dependency, repaired by explicitly registering Customer models; a fresh r7 database eliminated unrelated fixed-fixture collisions and upgraded cleanly to `f4d3b82a9e5c`. The combined Beacon/authorization run passes 143 tests with clean changed-production/new-test Ruff, legacy-test import ordering, MyPy, and diff gates. Eight unrelated pre-existing Ruff rules remain in the legacy authorization test body and were not mass-formatted.
38. `PAYROLL_PROTECTED_OUTPUT/FRONTEND_AUTHORIZATION`: The shared Payroll router reflected every protected conflict and raw `ValueError` across pay statements, artifact retrieval, reporting, compliance schemas, and filing packages. Those messages can contain Employee, SSN/routing, policy, or provider-adjacent details. The Payroll Administration route also initiated three protected readiness/report/schema queries before verifying both reporting-read and run-read authority. Payroll now returns fixed correlation-capable forbidden, refresh, correction, and owner-administrator recovery envelopes; a combined SSN/routing/provider canary is absent from every class. Frontend queries are explicitly disabled unless both read permissions are present, while the existing safe generic failure state remains non-reflective. All 103 Payroll tests pass on fresh r7; two route authorization tests, focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates pass.
39. `FINANCIAL_REPORTING_SAFE_OUTPUT/AUDIT_SAFETY/FRONTEND_AUTHORIZATION`: Financial Reporting reflected not-found and request-validation exceptions. More critically, ledger-integrity exception text was persisted verbatim as an Accounting posting-failure `error_code`, allowing SQL, constraint, Payroll, or protected source details to become durable audit evidence. The report UI also initiated a protected report request before checking report-read authority. Responses now use fixed terminal, correction, and reconciliation envelopes. Durable integrity evidence records only `ledger_integrity_failure`, carries safe report metadata and request digest, and propagates the request correlation identity (or a generated safe UUID) without exception text. The frontend query is disabled without permission and retains its non-reflective reconciliation guidance. All 9 Financial Reporting tests and 4 frontend route/API tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
40. `BUSINESS_ECONOMICS_SAFE_OUTPUT/STALE_FRONTEND_AUTHORIZATION`: Economics workspace validation and result lookup reflected domain details that can include Customer, Job, Payroll, or accepted-source evidence. Workspace query gating had been repaired previously, but once a result was selected, revoking Economics read authority left the nested detail query enabled. Both backend paths now emit fixed correlation-capable correction or terminal envelopes with protected canaries excluded. The result hook accepts explicit authority and disables immediately when permission is absent or stale, matching the workspace boundary. All 151 Business Economics tests and 3 route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
41. `PLATFORM_IDENTITY_ADMIN_SAFE_OUTPUT`: Password-reset confirmation reflected the configured password-policy exception, and Company administration reflected not-found, conflict, and validation details that can reveal permission, role, membership, or identity state. Both boundaries now emit fixed correlation-capable recovery envelopes: password-policy and administrative validation require user correction, administrative conflicts require refresh, and concealed administrative absence is terminal. Adversarial policy and administration canaries are absent from every response. A fresh r8 database upgraded from zero to `f4d3b82a9e5c`; the non-Redis Platform identity/administration run passes 30 tests with one Redis-dependent authentication test explicitly deselected, and focused Ruff, MyPy, and diff gates pass. The excluded test remains `ENVIRONMENT`: the unavailable configured `redis` hostname causes the rate limiter to fail closed rather than weakening authentication.
42. `MOBILE_ENGINEERING_SAFE_OUTPUT/PROVIDER_UNCERTAINTY`: The mobile-facing Engineering API reflected internal milestone, external-adoption, notification, realtime-resume, and workstream exception text. Provider repository readiness also returned its internal error as a message without the standard recovery vocabulary, inviting undifferentiated retry of a reconciliation condition. These boundaries now use fixed correlation-capable terminal and refresh envelopes, while repository-readiness uncertainty is explicitly `RECONCILIATION_REQUIRED`. A fresh r9 database upgraded from zero to `f4d3b82a9e5c`; all 31 mobile Engineering API tests pass, including safe recovery classification and cross-Company concealment, with clean focused Ruff, MyPy, and diff gates.
43. `PRIVILEGED_ENGINEERING_SAFE_OUTPUT/RECONCILIATION`: Repository authorization, bounded repository operation, and immutable owner-review APIs reflected conflict, evidence mismatch, eligibility, Git/provider-adjacent, and validation exception text at privileged approval and execution boundaries. They now emit fixed correlation-capable terminal, refresh, correction, or reconciliation envelopes without internal evidence. Repository-operation reconciliation is deliberately separated from ordinary conflict so clients do not retry uncertain repository mutation blindly. A canary covers every translated class. A fresh r10 database upgraded from zero to `f4d3b82a9e5c`; all 22 authorization/operation/review tests pass with clean focused Ruff, MyPy, and diff gates.
44. `TIMEKEEPING_PROTECTED_OUTPUT/CONTROLLED_EXECUTION_SAFE_OUTPUT`: Workday Time reflected authorization, lifecycle, idempotency, and validation exception text across employee punches, corrections, approvals, and Payroll input preparation. Controlled expired-result adoption likewise reflected provider/repository exception text. Timekeeping failures now use fixed correlation-capable terminal, refresh, and correction envelopes; controlled adoption uses a non-reflective refresh contract. Payroll/SSN/routing and provider/path canaries are absent from responses. A fresh r11 database upgraded from zero to `f4d3b82a9e5c`; all 23 Timekeeping and controlled-execution tests pass with clean focused Ruff, MyPy, and diff gates.
45. `MIGRATION_DURABLE_EXCEPTION_PRIVACY`: Financial operational migration intentionally hashes source identities before persisting exception evidence, but then stored raw `ParentResolutionError`, `FinancialValidationError`, or `MigrationRecordError` text beside the hash. Those messages can reconstruct protected source identifiers, financial references, or adapter details and defeat the hashing boundary. Estimate, Invoice, and Payment migration now persist fixed reason-specific detail while retaining machine-readable disposition/reason and the source digest. Tests assert missing-parent and validation detail is fixed and excludes synthetic source/reference evidence. A fresh r13 database upgraded from zero to `f4d3b82a9e5c`; all 5 operational-migration tests pass with clean focused Ruff, MyPy, and diff gates. An r12 rerun after a prior partial execution reproduced known non-cleaning fixture collisions and is classified `TEST_ISOLATION`, not product failure.
46. `CONTROLLED_PROVIDER_SAFE_OUTPUT`: The isolated controlled-execution provider reflected authority-registry and workspace-preparation exception text. Those exceptions can contain execution/lease identities, repository names, branches, filesystem paths, or provider configuration. Execute and authority-refresh conflicts now return a fixed refresh-classified envelope; workspace preparation rejection returns fixed owner-administrator guidance. Authentication remains fixed and fail-closed. All 27 controlled-provider tests pass, the provider boundary contains no remaining direct exception reflection, and focused Ruff, MyPy, and diff gates pass.
47. `ENGINEERING_CAPACITY_SAFE_OUTPUT/RECOVERY_SEMANTICS`: Engineering Capacity serialized every domain exception, including worker, credential, allocation, reservation, and provider state. It also exposed unavailable and reconciliation-required states through the same unclassified 409 shape as ordinary conflict. The boundary now emits fixed correlation-capable terminal, refresh, temporary-unavailable, reconciliation, and correction envelopes according to the concrete failure class; a provider/path canary is absent throughout. A fresh r14 database upgraded from zero to `f4d3b82a9e5c`; all 10 capacity API/service tests pass with clean focused Ruff, MyPy, and diff gates.
48. `WORKER_RECOVERY_SAFE_OUTPUT/NO_BLIND_RETRY`: Owner and authenticated-worker recovery acknowledgement endpoints reflected errors containing worker/execution/offer/lease lineage, journal reconciliation reasons, or local archive state. Both endpoints now return one fixed correlation-capable `RECONCILIATION_REQUIRED` envelope, preserving the deliberate no-blind-retry contract without disclosing internal evidence. The service's durable reconciliation and idempotency behavior is unchanged. All 7 recovery-acknowledgement tests pass with a worker credential/provider-journal canary excluded, and focused Ruff, MyPy, and diff gates pass.
49. `AUTHORITY_RECONCILIATION/AP_FRONTEND_AUTHORIZATION_COMPOSITION`: Protected authority `f0fb25e` correctly admitted Payroll reporting-read and AP report-read users, but the reconciled AP route admitted either operational read or report read while unconditionally requesting both the report-protected aging endpoint and the operational-read procurement-match queue. Thus each single-purpose reader triggered a forbidden cross-permission request. Aging is now enabled and rendered only for report-read authority; procurement matching is mounted only for AP operational-read authority. Direct tests prove report-only and operational-only partial workspaces issue only their authorized queries. Nine focused frontend tests, ESLint, TypeScript, production Vite build, and diff gates pass after protected-authority reconciliation.
50. `STALE_FRONTEND_AUTHORIZATION/OBJECT_READ`: Estimates, Invoices, and Payments list routes rendered a forbidden state without read permission but still initiated their protected list queries before returning. Invoice and Payment direct-detail routes did not gate valid object identities by read permission at all, so a stale or direct URL caused an unauthorized API attempt even though the backend remained authoritative. List and object hooks now accept the current read authority and disable immediately when absent; detail routes render an explicit forbidden state before loading. Tests prove no list/detail API function is called without its exact permission. Twelve focused route tests, ESLint, TypeScript, production Vite build, and diff gates pass.
51. `JOB_FRONTEND_AUTHORIZATION/SOD`: Jobs list and detail routes had no component-level read gate, the list exposed creation to read-only users, and every read-only Job detail rendered lifecycle mutation controls. Backend permissions remained authoritative, but stale/direct navigation caused forbidden reads and presented `JOB_MANAGE` and `JOB_EXECUTE` actions to `JOB_READ` users. Job queries now disable without read authority, creation requires manage authority, and lifecycle controls require execute authority. Tests prove read-only inspection remains available without mutation controls and direct unauthorized object reads are not requested. Ten focused Job route/lifecycle tests, ESLint, TypeScript, production Vite build, and diff gates pass.
52. `SCHEDULING_FRONTEND_AUTHORIZATION/CROSS_DOMAIN_COMPOSITION`: Scheduling list/detail routes relied on navigation hiding and initiated Appointment reads without current scheduling-read authority. Appointment detail also automatically queried Customer and Job identities using scheduling authority alone, and exposed Job creation without Job-manage authority. Scheduling reads now disable immediately without `SCHEDULING_READ`; Customer and Job projections are independently enabled only by their own read permissions; Job creation requires `JOB_MANAGE`. A scheduling-only user retains safe Appointment evidence while seeing explicit unavailable cross-domain projections rather than triggering forbidden requests. Sixteen focused Scheduling/Appointment/Job tests, ESLint, TypeScript, production Vite build, and diff gates pass.
53. `DISPATCH_FRONTEND_AUTHORIZATION/SOD`: Dispatch relied on navigation hiding, always loaded its board, composed `JOB_READ` automatically into every dispatcher, and exposed assignment/reconciliation controls to Dispatch read-only users. The board now disables without `DISPATCH_READ`; operational Jobs load only with independent `JOB_READ`; technician assignment and recovery controls require `DISPATCH_MANAGE`. A Dispatch-only reader retains assignment evidence without actionable controls or forbidden Job requests. All 8 Dispatch route tests pass with explicit read/manage/Job-read composition assertions, alongside ESLint, TypeScript, production Vite build, and diff gates.
54. `CUSTOMER_FRONTEND_AUTHORIZATION/SOD`: Customer list and direct-detail routes relied on navigation hiding, so stale or direct navigation mounted protected queries without `COMPANY_CUSTOMER_READ`. Customer read-only users also received creation, edit, archive, consent, property, contact, and note controls that require `COMPANY_CUSTOMER_MANAGE`; open forms could remain actionable after permission revocation. Both routes now fail before mounting data components without read authority. Creation and every detail mutation surface, including already-open forms and confirmation, require current manage authority while read-only evidence remains available. Twelve focused Customer component/route tests pass with direct query-mount and read/manage composition assertions, alongside ESLint, TypeScript, production Vite build, and diff gates.
55. `COMMAND_CENTER_CROSS_DOMAIN_AUTHORIZATION/INTEGRITY`: The shared Command Center unconditionally requested Analytics, Beacon, and Job evidence for every authenticated user. This both generated forbidden cross-permission requests and risked representing a disabled Beacon request as an authoritative empty queue. Analytics/Beacon reads now require `COMPANY_ANALYTICS_READ`, while the Job count independently requires `COMPANY_JOB_READ`; absent Beacon authority is represented explicitly rather than inferred as no signals. Four focused route/hook tests pass, including exact disabled-query composition, alongside ESLint, TypeScript, production Vite build, and diff gates.
56. `MISSION_CONTROL_FRONTEND_AUTHORIZATION/SAFE_OUTPUT`: Mission Control relied on navigation hiding and unconditionally requested protected Analytics evidence. Its failure panel also rendered the raw client error message, permitting backend or transport-adjacent details to cross the web boundary. The route now disables Analytics retrieval without `COMPANY_ANALYTICS_READ`, renders an explicit forbidden state, and uses fixed non-reflective failure guidance. A synthetic protected-source canary is absent from rendered output. Seven combined Mission Control/Command Center tests pass with ESLint, TypeScript, production Vite build, and diff gates.
57. `ENGINEERING_FRONTEND_AUTHORIZATION/SOD`: The active mobile Engineering list and direct-detail routes mounted command, notification, approval, roadmap, and object reads without `COMPANY_ENGINEERING_COMMAND_READ`. The shared navigation also exposed Capacity without its independent read permission, while read-only command users received workstream manage, command approval/cancellation, and immutable-result approval controls based only on resource state. Engineering queries now disable before protected reads without command authority, Capacity is independently visible only with `COMPANY_ENGINEERING_CAPACITY_READ`, workstream actions require command-manage, approvals and result decisions require command-approve, and rejection/revision requires command-manage. Read-only evidence remains visible. Twelve focused mobile Engineering tests pass, including unauthorized query composition and read/manage/approve separation, with ESLint, TypeScript, production Vite build, and diff gates.
58. `ENGINEERING_CAPACITY/ROADMAP_FRONTEND_SOD`: Within the authorized Engineering workspace, Capacity read authority exposed policy, worker, reservation, allocation, and reconciliation mutations that require `COMPANY_ENGINEERING_CAPACITY_MANAGE`; roadmap read authority likewise exposed milestone decisions that require command-approve. Capacity readers now receive explicit read-only aggregate evidence with no mutation inputs or controls, while the full control surface remains available only to capacity managers. Milestone action and confirmation controls require current command-approve authority, including stale-permission closure. Thirteen focused mobile Engineering tests pass, including read-only Capacity evidence, with ESLint, TypeScript, production Vite build, and diff gates.
59. `ENGINEERING_NOTIFICATION_FRONTEND_SOD`: Mission notification evidence is protected by command-read, but read-only users were presented Mark read, Acknowledge, and Archive controls whose API endpoints require command-manage. Notification evidence remains visible to command readers while all lifecycle controls now require current `COMPANY_ENGINEERING_COMMAND_MANAGE`, including stale-permission rerenders. Fourteen focused mobile Engineering tests pass, with a direct read-only notification assertion plus ESLint, TypeScript, production Vite build, and diff gates.
60. `ADMINISTRATION_FRONTEND_AUTHORIZATION/SOD`: Role Administration initiated protected role reads for any authenticated visitor and exposed Grant/Remove controls to role readers without the independent permission-manage authority. Roles and nested permission evidence now disable before retrieval without `COMPANY_ROLE_READ`; role readers retain inspection/search while mutations and already-open confirmation state require current `COMPANY_PERMISSION_MANAGE`. Direct tests prove no role or permission API call occurs without role-read and that read-only role evidence has no mutation controls. Ten Administration tests pass with ESLint, TypeScript, production Vite build, and diff gates.
61. `ADMINISTRATION_PARTIAL_WORKSPACE_COMPOSITION`: QuickBooks sandbox administration and read-only Migration readiness intentionally use `COMPANY_ADMINISTER`, independently of role-read. A whole-route role-read gate would deny these valid single-purpose administrators. Administration now composes the workspace by authority: Company administrators retain their QBO/Migration panels without triggering role APIs, while role administration renders only with role-read and permission mutations remain separately gated. A direct partial-authority test proves both authorized panels render and both unauthorized role calls remain absent; the ten-test Administration suite and frontend static/build gates pass.
62. `CUSTOMER_API_RECOVERY/FRONTEND_SAFE_OUTPUT`: Customer not-found and lifecycle-conflict responses used safe fixed text but lacked the standard recovery vocabulary, while Customer form, consent, and mutation panels rendered generic Axios/backend messages directly. Customer absence now returns a correlation-capable terminal envelope and invalid lifecycle transitions return a refresh-classified conflict envelope. All Customer frontend failure panels use the non-reflective operator contract; a SQL/protected-source canary is absent from rendered output. A fresh r16 database upgraded from zero, eight Customer backend tests pass, ten focused frontend tests pass, and focused Ruff, MyPy, ESLint, TypeScript, production build, and diff gates are clean.
63. `SHARED_FRONTEND_SAFE_OUTPUT`: The shared operator-error formatter accepted unstructured backend detail as fallback text for generic 400, 409, and 422 responses. That made every otherwise-safe UI consumer vulnerable to a newly integrated or legacy endpoint reflecting SQL, provider, credential, or protected-domain detail. Generic failures now use fixed correction/refresh guidance and accept server-provided text only from the structured safe-envelope shape. Canary tests cover all three status classes. The full frontend suite passes 95 files / 308 tests with repository-wide ESLint, TypeScript, production build, and diff gates.
64. `IDENTITY_ADMIN_RECOVERY_CONTRACT`: Identity administration correctly used fixed non-reflective messages for missing resources, token state, lifecycle conflicts, and validation failures, but omitted the standardized machine-readable recovery contract and request correlation. The boundary now emits correlation-capable safe envelopes: absence is terminal, token/state conflicts require refresh, and validation requires user correction. Duplicate revocation and revoked-token API assertions bind the structured contract. All 6 Identity administration API tests pass on the fresh r16 schema with focused Ruff, MyPy, and diff gates clean.
65. `EMPLOYEE_ENGINEERING_RECOVERY_CONTRACT`: Employee self-service identity-readiness and mobile Engineering command-absence responses used non-reflective text but omitted recovery classification and correlation. Employee identity mapping cannot be corrected by the employee, so it now returns `OWNER_ADMIN_ACTION_REQUIRED`; a concealed missing Engineering command returns `TERMINAL_FAILURE`. Direct HTTP assertions bind both safe-envelope contracts. All 24 Employee Operations and Engineering status tests pass on r16 with focused Ruff, MyPy, and diff gates clean.
66. `ACCOUNTING_SOD_RECOVERY_CONTRACT`: Period close and reopen-approval performed direct permission composition checks but bypassed Accounting's standardized safe failure translator, returning unclassified bare 403 strings. Both endpoints now use the fixed, correlation-capable forbidden envelope with owner-administrator recovery. Direct endpoint tests prove period-manage alone and finance-approve alone are each insufficient to close, and period-manage does not imply reopen approval. All 8 Accounting API-boundary tests pass with focused Ruff, MyPy, and diff gates clean.
67. `JOB_QUERY_RECOVERY_CONTRACT`: An incomplete Job date-range filter bypassed the existing safe Job validation translator and returned a bare 422 string, unlike other domain query validation. The router now applies the same fixed, correlation-capable `USER_CORRECTION_REQUIRED` envelope before query execution. A direct API assertion binds the structured response. All 58 Jobs API tests pass on r16 with focused Ruff, MyPy, and diff gates clean.
68. `AUDIT_BRANCH_RECOVERY_CONTRACT`: Explicit Audit requests for an unauthorized Branch failed closed but returned an unclassified bare 403 string. The API now emits a fixed, correlation-capable forbidden envelope with `OWNER_ADMIN_ACTION_REQUIRED`, while repository scoping remains unchanged. A direct protected-identity canary proves internal denial evidence is not reflected. All 13 launch-control and Audit tests pass on r16 with focused Ruff, MyPy, and diff gates clean.
69. `AUDIT_EVENT_TAX_IDENTITY_SAFETY`: The shared recursive sensitive-field classifier covered bank credentials, tokens, and protected source payloads but did not recognize SSN/TIN tax-identity aliases. Because Business Events intentionally admit ordinary personal fields, nested tax identifiers could pass event validation and Audit coverage did not prove their rejection. SSN, TIN, EIN, social-security-number, taxpayer-identification-number, employer-identification-number, and tax-id aliases are now secret-classified. Nested Audit and Business Event canaries fail before persistence. All 28 focused security/launch-control tests pass with focused Ruff, MyPy, and diff gates clean; one pre-existing Starlette TestClient deprecation warning remains.
70. `PROCUREMENT_MATCH_DATABASE_INVARIANT_PARITY`: Procurement Match lines derived positive/nonnegative quantities, costs, and AP amounts in application code, including `net_accepted = received - returned`, but PostgreSQL did not protect those durable facts. Named checks now require positive ordered/billed quantities, nonnegative received/returned quantities and amounts, returns not exceeding receipts, and exact net-accepted derivation. PostgreSQL directly rejects a synthetic corrupted net quantity. Migration `g5e4c93b0f6d` passed zero-to-head upgrade on fresh r17, current=head, single-head, drift, downgrade/re-upgrade, and the full 90-test Purchasing, Procurement Matching, and Inventory regression with clean focused Ruff, MyPy, and diff gates.
71. `PAYROLL_REPORTING_UNBOUNDED_QUERY`: Payroll reporting snapshots, compliance schemas, and report filing packages loaded every Company-scoped row with no query bound, creating an avoidable scale and protected-response risk as evidence accumulates. Each list now publishes backward-compatible `limit`/`offset` parameters with defaults of 100/0, a maximum page size of 200, database-side bounds, and deterministic timestamp/identity tie-break ordering. OpenAPI tests bind all three contracts. The full 104-test Payroll suite passes on r17 with focused Ruff, MyPy, and diff gates clean.
72. `INVENTORY_CYCLE_HISTORY_UNBOUNDED_QUERY`: Inventory cycle-count history loaded every authorized-Branch session and every associated entry into memory. The endpoint, service, repository contract, and PostgreSQL query now enforce backward-compatible pages with defaults of 100/0 and a maximum size of 200; internal callers cannot bypass the bound. Session ordering is deterministic and entries remain batched in one query for only the selected page, avoiding N+1 behavior. A two-session test proves page separation and exact lifecycle Event counts. All 31 Inventory tests pass on r17 with focused Ruff, MyPy, and diff gates clean.
73. `ADMIN_MEMBERSHIP_UNBOUNDED_QUERY`: Company administration loaded every membership for the Company into one response. The endpoint and service now enforce backward-compatible pagination with defaults of 100/0, a maximum page size of 200, deterministic creation/identity ordering, and database-side bounds; direct clients cannot request an oversized page. All 12 Company administration tests pass on r17 with focused Ruff, MyPy, and diff gates clean.
74. `PENDING_OM2A_BATCHED_WORKSPACE_READ_MODEL`: Static query-path reproduction shows Purchasing workspace loads five unbounded top-level collections and serializes each PO through `_item`, which performs at least nine repository queries per PO plus one query per receipt and return. A superficial page limit would retain the pathological N+1 behavior, while removing nested evidence changes the Supply Chain workspace contract. OM2-C records this exact owning-lane gate without claiming repair; no Production load test was performed.
75. `CRITICAL_BUSINESS_EVENT_CROSS_TENANT_BOUNDARY`: The mounted Business Event API required only a database dependency. Unauthenticated callers could list globally unscoped event payloads and publish arbitrary events carrying caller-selected Company, Branch, and user identities; the direct write was also explicitly exempt from idempotency despite creating authoritative append-only evidence. Direct HTTP publication has been removed—domains retain transaction-bound internal staging—and both read endpoints now require `COMPANY_AUDIT_READ`. Queries bind Company identity and expose only Company-wide plus authorized-Branch events unless the membership has all-Branch authority. Tests prove unauthenticated 401, publication 405, permitted Company/Branch visibility, and exclusion of denied Branch and foreign Company canaries. The refreshed mutation registry exactly covers all 249 remaining mutations. All 20 Events/idempotency-standard tests and a fresh-r18 178-test non-Redis Platform regression pass with focused Ruff, MyPy, and diff gates clean.
76. `TEST_ISOLATION/BOOTSTRAP_MODEL_REGISTRY`: Platform bootstrap tests call global `Base.metadata.create_all()` but registered only a partial model graph. New Customer/HCP lineage foreign keys therefore passed only when unrelated test collection happened to import operational migration, Customer migration, Employee, Jobs, and Estimate targets first. The fixture now explicitly loads the application model graph plus the operational-migration models not mounted by `app.main`, making its global-schema assumption deterministic. The file passes 7/7 alone. A reused-r17 fixed permission canary collision was separately eliminated by fresh r18; the non-Redis Platform result is 178 passed, 1 explicitly deselected live-Redis test, and one pre-existing TestClient deprecation warning.
77. `HTTP_AUTHENTICATION_BOUNDARY_REGRESSION_ARCHITECTURE`: The critical Event exposure existed because no test treated the set of non-Bearer HTTP operations as a security contract. A new OpenAPI qualification enumerates the exact reviewed surface: health, authentication/recovery, activation, safe contract metadata, QBO callbacks, root, and separately credentialed worker transport. Any new operation lacking Bearer authentication now fails unless the reviewed allowlist changes explicitly. The test also binds Event reads as secured and direct Event publication as absent. It and isolated bootstrap qualification pass 8/8 with Ruff and diff gates clean.
78. `PROTECTED_API_CACHE_BOUNDARY`: Most protected JSON endpoints inherited no explicit cache directive; only selected Payroll artifacts, Migration projections, and QBO paths set `no-store`. Shared security middleware now defaults every `/api/` response—including failures and public recovery boundaries—to `Cache-Control: private, no-store` when the endpoint has not declared its own policy. Non-API responses remain unchanged and endpoint-specific policies such as `no-cache` are preserved. All 8 enterprise security-hardening tests pass with focused Ruff, MyPy, and diff gates clean.
79. `NON_PRODUCTION_PERFORMANCE_BASELINE`: Repeatable local evidence now covers warmed health middleware and representative Customer, Job, Scheduling, Purchasing, Payroll reporting, and Migration paths. Methodology, sample sizes, phase timings, host-local limitations, and the explicit non-SLO/non-Production classification are recorded in Qualification results. The representative functional tranche passed 90/90; no speculative optimization was made from fixture-heavy timing alone.
80. `MIGRATION_OBJECT_BINDING_DEFENSE_IN_DEPTH`: Appointment migration resolved a Company-scoped source Job identity and then loaded its target Job by UUID alone. The current schema's composite foreign key prevents a newly persisted cross-Company identity, but repository retrieval now independently requires the active Company so legacy, corrupted, or incorrectly substituted identity evidence cannot cross the service boundary. A SQL-contract test binds both Job and Company predicates. After the prior isolated database had been removed, fresh isolated r19 upgraded from zero to the single head `g5e4c93b0f6d`; all 6 operational Migration repository, dry-run/rerun/reconciliation, and rollback tests pass with focused Ruff and diff gates clean.
81. `MIGRATION_CROSS_COMPANY_RESUME_AUTHORIZATION`: Provider-neutral operational resume loaded a run by UUID and validated digest, lineage, generation, and state, but did not bind the run to the caller's active Company or Branch. Knowledge of a foreign run UUID plus matching source content could therefore change its lifecycle and process evidence under the wrong authorization context. Resume admission now requires exact Company and Branch authority before any run mutation. An adversarial two-Company test proves the foreign failed run remains unchanged. A freshly recreated isolated r19 upgraded from zero to `g5e4c93b0f6d`, and all 7 operational Migration repository, resume-isolation, dry-run/rerun/reconciliation, and rollback tests pass with focused Ruff, MyPy, and diff gates clean.
82. `CRITICAL_PAYMENT_INTENT_OBJECT_BINDING`: Collection trusted caller-selected Customer and optional Invoice UUIDs after checking only Branch access. The database used a global Customer foreign key and no Invoice foreign key, so Company A could persist a provider-bound intent referencing Company B's Customer or an Invoice outside the selected Company/Branch/Customer composition. Collection now resolves the Customer in exact Company scope and, when supplied, resolves the Invoice by exact Company, Branch, Customer, and identity before intent creation or provider contact. Migration `h6f5d04c1a7e` replaces the global Customer reference with a composite tenant foreign key and adds an optional composite Invoice foreign key. The adversarial service test proves no intent and zero provider calls; direct corrupted persistence is rejected by PostgreSQL. Fresh r19 zero-to-head, downgrade/re-upgrade, current=head, single-head, and drift checks pass; all 25 Payments/Invoicing tests pass with four pre-existing shared-connection transaction warnings and clean focused Ruff, MyPy, and diff gates.
83. `PAYMENT_APPLICATION_CONCURRENT_REPLAY`: Receipt application checked replay while holding the receipt, released that transaction while the Invoice application committed, and then reacquired the receipt without rechecking replay. Two identical requests could both cross the Invoice boundary and sequentially decrement the receipt despite one idempotent Invoice authority. The locked post-Invoice transaction now rechecks the application key and immutable request digest before changing receipt balances. A deterministic two-engine-session barrier test forces both requests past the original check and proves one 30.00 application, one Receipt Event, and an exact 50.00 remaining balance from 80.00. The complete 17-test Payments suite passes with focused Ruff, MyPy, and diff gates clean.
84. `PAYMENT_ACCOUNTING_RECEIPT_REPLAY_INTEGRITY`: Payment Accounting posting receipt replay compared only journal identity and status. Reusing the same source Event with a different journal version, Accounting policy, effective date, or posting timestamp silently returned prior authority and weakened immutable posting provenance. Replay now binds every persisted receipt field. Exact replay returns one identity, while six independently changed authority dimensions fail as typed Payment conflicts. The complete 18-test Payments suite passes with focused Ruff, MyPy, and diff gates clean.
85. `PAYMENT_DISPUTE_PROVIDER_PROVENANCE/REPLAY`: Dispute recording accepted `provider_dispute_id` but never validated, persisted, or included it in replay authority; an existing idempotency key returned prior authority despite a changed amount, provider identity, or provider evidence digest. Dispute events now persist the provider reference and a canonical complete-request digest, replay compares that digest, and a database check requires both for every new dispute event. Migration `i7g6e15d2b8f` truthfully marks historical disputes as `legacy-evidence-unavailable` with a deterministic legacy digest, causing modern replay to fail closed rather than inventing provider provenance. A synthetic pre-migration dispute upgrades successfully; exact replay mutates 20.00 once while three contradiction dimensions fail. Fresh r19 zero-to-head, downgrade/legacy-upgrade, current=head, single-head, and drift checks pass; all 19 Payments tests pass with focused Ruff, MyPy, and diff gates clean.
86. `PAYMENT_SETTLEMENT_REPLAY_INTEGRITY`: Settlement replay keyed correctly on Company/provider/merchant/payout identity but compared only the caller-supplied evidence digest. Reusing that digest could silently accept changed currency, settlement date, gross, refund, dispute, fee, adjustment, or net economic truth. Replay now compares every persisted input before returning authority. Exact replay returns one settlement while eight independently altered evidence dimensions fail as typed Payment conflicts. The complete 20-test Payments suite passes with focused Ruff, MyPy, and diff gates clean.
87. `PAYMENT_WEBHOOK_RECONCILIATION_ROLLBACK/AUDIT_IDENTITY`: Contradictory webhook evidence staged a reconciliation exception and immediately raised inside the transaction, rolling back both the exception and Business Event. The shared exception helper also relied on an insert-time UUID default while staging its Event before flush, leaving historical Event `entity_id` values null. Webhook replay now compares event type, evidence digest, secret version, and allowlisted evidence; contradiction commits one idempotent exception before returning a typed conflict. New exceptions receive their UUID before Event staging, so audit evidence links exactly to durable reconciliation authority. Repeating the same contradiction creates neither a duplicate exception nor Event. The complete 21-test Payments suite passes with focused Ruff, MyPy, and diff gates clean; existing null-linked historical Events were not rewritten.
88. `PAYMENT_RECEIPT_UNBOUNDED_QUERY`: The protected receipt list loaded and serialized every authorized-Branch receipt with timestamp-only ordering. HTTP and service contracts now enforce backward-compatible 100/0 defaults, a maximum page size of 200, nonnegative offsets, database-side bounds, and captured-time/UUID deterministic ordering; internal callers cannot bypass the bound. OpenAPI assertions bind the public contract, and a three-receipt test proves disjoint stable pages plus oversized-request rejection. The complete 23-test Payments suite passes with focused Ruff, MyPy, and diff gates clean.
89. `INVOICE_AP_AGING_UNBOUNDED_QUERY`: Invoice listing and AP aging loaded every authorized-Branch economic record into protected responses. Both HTTP and service contracts now enforce backward-compatible 100/0 defaults, a maximum page size of 200, nonnegative offsets, database-side limits, and deterministic UUID tie-break ordering after their existing business sort. OpenAPI assertions bind both public contracts and direct service tests prevent internal bypass. The combined 20-test Invoicing/AP suite passes with four pre-existing shared-connection transaction warnings plus clean focused Ruff, MyPy, and diff gates.
90. `INVOICE_AP_BRANCH_RECOVERY_CONTRACT`: Invoicing and AP domain exceptions used standardized recovery envelopes, but direct Branch concealment and nested AP line-Branch denial bypassed those translators and returned bare 404 strings. All Invoice/AP Branch and concealed-object denials now use fixed correlation-capable `not_found` envelopes with `TERMINAL_FAILURE`, retaining existence concealment without reflecting submitted identities. Direct canary assertions bind the response contract. The combined 21-test Invoicing/AP suite passes with four pre-existing shared-connection transaction warnings plus clean focused Ruff, MyPy, and diff gates.
91. `PROCUREMENT_METRIC_DECIMAL_SAFETY`: Vendor performance converted exact receipt-to-issue timedeltas to binary floating-point days, averaged them as floats, and converted the result back through string to Decimal. The metric now derives exact integer microseconds, divides and averages entirely in Decimal, and quantizes only at the published two-decimal boundary. A fractional-day/microsecond regression binds the exact conversion while the existing functional report remains 1.00 days. All 10 Procurement Matching tests pass with focused Ruff, MyPy, and diff gates clean; no business rounding policy was changed.
92. `TRUSTED_PROXY_RECOVERY_CONTRACT`: Trusted-proxy middleware correctly rejected spoofed peers and malformed forwarding chains before authentication, but returned bare error strings outside the standardized recovery vocabulary. All forwarding-header rejection paths now emit fixed non-reflective `validation` envelopes with `USER_CORRECTION_REQUIRED` and the active safe correlation UUID. Tests prove both untrusted and malformed canaries are absent from output and response correlation matches `X-Request-ID`. All 8 enterprise security-hardening tests pass with focused Ruff, MyPy, and diff gates clean; proxy trust semantics were not weakened.
93. `DISPATCH_REPLACEMENT_RECOVERY_CONTRACT`: Primary-assignment replacement required an expected version but handled its absence before the domain translator, returning a bare 422 string. The precondition now uses Dispatch's fixed correlation-capable `validation` envelope with `USER_CORRECTION_REQUIRED`; the internal reason is not reflected. A direct endpoint assertion binds the contract. All 14 Dispatch tests pass with four pre-existing shared-connection transaction warnings plus clean focused Ruff, MyPy, and diff gates.
94. `PAYROLL_STORAGE/REPORT_RECOVERY_CONTRACT`: Missing protected pay-statement/report storage returned bare 503 configuration strings, and absent reporting snapshots or filing-package parents returned bare 404 strings. Storage absence now emits a fixed correlation-capable `dependency_unavailable` envelope with `OWNER_ADMIN_ACTION_REQUIRED`; concealed report absence emits terminal `not_found`. Tests bind both classifications and prove configuration wording is not exposed. The full 105-test Payroll suite passes with focused Ruff, MyPy, and diff gates clean.
95. `ESTIMATE_PURCHASING_PRECONDITION_RECOVERY`: Estimate Branch denial, concealed detail absence, invalid tax-policy windows, and unsupported Purchasing requisition actions bypassed their domains' safe translators and returned bare strings. Estimate Branch denial now emits fixed `forbidden`/owner-action recovery; concealed Estimate and unsupported action identities emit terminal `not_found`; tax-window validation requires user correction. Direct canaries prove submitted identities/actions are not reflected. The complete Estimate suite plus Purchasing API boundary passes 36/36 with 13 pre-existing shared-connection transaction warnings and clean focused Ruff, MyPy, and diff gates.
96. `INTERNAL_FAILURE_HTTP_MISCLASSIFICATION`: Communications, Price Book, Estimates, Purchasing, Jobs, Scheduling, Dispatch, and Payroll safely redacted unexpected domain failures but returned HTTP 400 while declaring `internal_failure`, incorrectly telling clients that a server-side failure was a malformed request. Generic fallbacks now return HTTP 500 while preserving fixed messages, correlation identity, and the existing owner/terminal recovery classifications; validation failures remain 422. Boundary tests bind every affected translator and prove protected exception details are not reflected. All 93 focused API tests pass with clean focused Ruff, MyPy, and diff gates.
97. `OWNER_ACTIVATION_CLAIM_AUDIT/RECOVERY`: Protected authority `8e21480` introduced a non-Production, owner-mediated one-time activation-token claim with tenant/Branch binding and row-locked replay rejection, but successful disclosure changed only envelope state and left no actor-attributed audit record; its controlled failures also inherited bare-string Onboarding responses. The owner path now transactionally stages exactly one secret-free Audit record linked to the request, invitation, actor, Company, and Branch. Replay remains fail-closed and cannot duplicate audit evidence. Onboarding authorization/conflict responses now use fixed correlation-capable recovery envelopes without reflecting token/provider detail. Reconciliation also removes the already-retired direct Event publication from the advanced authority's registry, yielding exactly 250 current classified mutations and fingerprint `d57ed8afd5a3af7975b537b3c8760e6ebba6bb65f95be9aafedfd59bf3f81823`. All 45 focused Onboarding/idempotency/authentication/sensitive-output/security tests pass with one pre-existing Starlette deprecation warning; focused Ruff, MyPy, single-head/current-head, Alembic drift, and diff gates are clean.
98. `ONBOARDING_BRANCH_OBJECT_AUTHORIZATION`: Identity Onboarding initiation and the newly integrated owner-claim path enforced active Branch authority, but detail, revoke, and reissue loaded a caller-supplied request by UUID and Company only. A same-Company administrator restricted to another Branch could inspect or invalidate valid onboarding authority. All three operations now require current Branch access and preserve the same concealed conflict contract. An adversarial valid foreign-Branch request fails for claim, detail, revoke, and reissue, after which the authorized owner still observes the original `invited` state. The 45-test focused security set passes with one pre-existing Starlette deprecation warning and clean focused Ruff, MyPy, and diff gates.
99. `OWNER_CLAIM_SECRET_RETENTION/CONCURRENCY`: Owner-mediated activation-token claim decrypted and returned the token but retained its ciphertext and nonce in a `claimed` envelope until later activation, revocation, or reissue, unnecessarily preserving recoverable secret material after delivery was complete. The owner path now atomically clears ciphertext and nonce, marks the envelope `delivered`, timestamps destruction, and stages its secret-free Audit record before returning the in-memory token; the separate provider boundary retains its claim/complete handshake. A true two-session PostgreSQL race proves exactly one plaintext winner, one typed conflict, one delivered-and-erased envelope, and one Audit record with no token reflection. The 46-test focused Onboarding/idempotency/authentication/sensitive-output/security set passes with one pre-existing Starlette deprecation warning and clean focused Ruff, MyPy, and diff gates.
100. `EMPLOYEE_PAY_STATEMENT_UNBOUNDED_QUERY`: The protected employee pay-statement list loaded every issued statement, and the payroll-status projection repeated the same full-history load only to derive an exact count and current item. The list now publishes backward-compatible `limit=100`/`offset=0` parameters with a maximum of 200, validates direct service callers, applies database bounds, and orders deterministically by creation time and UUID. Payroll status now performs an exact database count plus a one-row current projection, preserving its response semantics without materializing history. OpenAPI and persistence tests bind page separation, oversized rejection, exact count, and current identity. The full 106-test Payroll suite passes with clean focused Ruff, MyPy, and diff gates.
101. `LIA_MUTATION_REGISTRY/TRUTHFUL_FEEDBACK`: Protected authority `5fbfda1` introduced LIA `/ask` and `/feedback` POST operations without entries in the authoritative mutation registry, causing the exact-surface qualification to fail. Feedback also returned `RECORDED_AS_QUALITY_EVIDENCE` despite intentionally retaining no durable transcript or feedback until owner policy exists. Both non-authoritative POST boundaries are now explicit reviewed exemptions: ask generates a fresh authorization-resolved response without business mutation, while feedback is ephemeral telemetry without a durable receipt. Its response truthfully reports `EPHEMERAL_TELEMETRY_ACCEPTED`. The combined registry classifies exactly 252 current mutations with fingerprint `7100d2ae5276cc5fcf4524903b6400c895ff06025c353853a4a25c933c6140c3`; durable retention remains a documented owner-policy gate.
102. `LIA_BRIEFING_DOMAIN_MISCLASSIFICATION`: The fixed owner briefing promises evidence across permitted domains, but the generic word `today` was classified as Scheduling and silently restricted retrieval to Appointments. Generic today/readiness questions now select all currently authorized adapters; explicit schedule, appointment, or dispatch language still selects Scheduling. A direct adapter-selection test proves a Job+Invoice reader sends both domains to retrieval. The focused LIA/platform security set passes 42/42 with one pre-existing Starlette deprecation warning and clean Ruff, MyPy, JSON, and diff gates.
103. `LIA_FRONTEND_FAILURE_RECOVERY`: LIA readiness and owner-briefing failures rendered safe non-fabricating alerts but offered no recovery action. Both failure states now retain the workspace and expose semantic retry buttons bound to their respective queries. Component tests invoke and verify both recovery callbacks. The LIA route passes 2/2 tests, focused ESLint, repository TypeScript, and the production Vite build.
104. `LIA_AUDIT_COMPLETENESS/PRIVACY`: LIA emitted safe request metadata only after evidence-backed `KNOWN` responses; injection/exfiltration refusals, unauthorized domain requests, high-impact proposals, fabrication pressure, and unavailable evidence all returned before telemetry. Response construction now emits exactly one uniform metadata-only audit line for every truth classification. A refusal canary proves request identity and classification remain observable while the complete prompt, private-key wording, answer, and protected payload text are absent. The focused LIA/platform security set passes 43/43 with one pre-existing Starlette deprecation warning and clean Ruff, MyPy, and diff gates.
105. `IDENTITY_ONBOARDING_FRONTEND_RECOVERY`: Protected authority `824ff2c` integrated the Preview Employee onboarding administration control and expanded governed LIA enterprise-domain classification. The onboarding form correctly blocks until the canonical Employee role and required permissions are verified, but a temporary role/permission API failure collapsed into a permanent blocked state with no recovery action. The blocked state now exposes an explicit readiness retry that re-runs the complete role/permission qualification without retaining protected input or weakening authorization. A deterministic fail-then-succeed component test proves recovery. Merge resolution preserves the independently qualified Identity Onboarding, QBO sandbox, Migration, and role-administration surfaces. The focused backend LIA/registry set passes 21/21; frontend Administration/Onboarding/LIA passes 16/16 with focused ESLint, MyPy, repository TypeScript, production Vite build, and diff gates clean.
106. `CUSTOMER_CONSENT_HISTORY_UNBOUNDED_QUERY`: Customer consent is append-only Business Event evidence, but the protected per-Customer endpoint loaded and serialized the complete history. HTTP and service contracts now enforce backward-compatible `limit=100`/`offset=0` pagination with a maximum of 200, database-side bounds, and deterministic occurred-at/UUID ordering; direct internal callers cannot bypass the limit. Tests prove newest/oldest page separation, public oversized-request rejection, internal oversized-call rejection, and unchanged cross-Company concealment. The full 30-test Customer suite passes with clean focused Ruff, MyPy, and diff gates. Contacts, locations, and note history remain part of the existing aggregate detail contract and were not silently truncated.
107. `IDENTITY_ONBOARDING_INITIATION_BRANCH_AUTHORIZATION`: Identity Onboarding owner claim, detail, revoke, and reissue were Branch-bound, but initiation accepted any active Branch belonging to the Company without requiring it in the administrator's authorized Branch set. The integrated frontend displayed only authorized Branches, yet a direct API caller with a valid foreign-Branch UUID could create the User, Membership, Employee, invitation, and role authority there. Initiation now rejects inaccessible Branch identity before digest construction or transaction mutation. An adversarial persisted same-Company foreign Branch creates no onboarding request. The combined 47-test Onboarding/idempotency/authentication/sensitive-output/security set passes with one pre-existing Starlette deprecation warning and clean focused Ruff, MyPy, and diff gates.
108. `IDENTITY_ONBOARDING_DATABASE_TENANT_INVARIANTS`: Durable onboarding requests protected Company+Membership composition but referenced Branch and Employee through global UUID foreign keys. PostgreSQL could therefore accept Company A request evidence pointing at Company B Branch or Employee truth if an application boundary regressed. Migration `j8h7f26e3c9a` replaces both global references with named Company+Branch and Company+Employee composite foreign keys, matching existing unique tenant identities. PostgreSQL directly rejects a synthetic cross-Company Branch substitution and exposes both named constraints. The 48-test focused Onboarding/platform security set passes with one pre-existing Starlette warning; r19 downgrade/re-upgrade and drift pass, and fresh isolated r20 upgrades from zero to the single current head with no drift. Focused Ruff, MyPy, and diff gates are clean.
109. `MIGRATION_REVISION_COLLISION/LUMINARY_RECONCILIATION`: Protected authority `be7f679` introduced Luminary using revision identity `g5e4c93b0f6d`, which OM2-C had independently assigned to Procurement Match constraints. The duplicate identity made the Alembic graph ambiguous and unloadable. Protected Luminary history remains unchanged; the non-central Procurement migration is relocated forward as idempotent revision `k9i8g37f4d0b`, preserving already-qualified databases while applying all three constraints to fresh databases. Isolated r21 upgrades from zero through the combined graph, reports one head/current=head/no drift, and cleanly downgrades/re-upgrades k9. The complete 9-test Procurement Match suite passes, including direct constraint metadata and database enforcement.
110. `IDENTITY_ONBOARDING_CONCURRENT_EXACT_REPLAY`: Initiation performed its absent-row replay lookup before acquiring the Company advisory lock. Two exact concurrent requests could both observe absence; the loser then reached the unique constraint and returned a typed conflict instead of the existing authority. The Company transaction lock now precedes the authoritative lookup. A true two-session PostgreSQL test proves both callers receive the same request, Employee, and Membership identities and exactly one request persists. Both onboarding concurrency tests pass.
111. `LUMINARY_API_FAILURE/FRONTEND_RECOVERY`: Luminary validation and concealed-not-found paths returned bare details without the standard recovery vocabulary or correlation identity, while unexpected service failures lacked a fixed safe envelope. The frontend treated every briefing error—including transport/500—as “not analyzed,” offered analysis during an outage, and provided no read retry. All Luminary boundaries now emit fixed correlated `SafeFailure` responses: validation requires user correction, absence is terminal, and unexpected failure is temporarily unavailable. The UI offers analysis only for HTTP 404 and otherwise preserves the workspace with an explicit briefing retry. Canary tests prove internal details are not reflected; two component tests bind the recovery distinction.
112. `LUMINARY_TENANT_LINEAGE/PROJECTION_INTEGRITY`: Luminary Branch and supersession references used global UUID foreign keys, permitting Company A findings or briefings to reference Company B Branch/predecessor truth if an application boundary regressed. Briefing projection also resolved JSON finding identities without Company/Branch predicates and silently omitted missing authority. Migration `l0j9h48g5e1c` replaces all four references with named composite Company constraints. Direct PostgreSQL attacks prove cross-Company finding Branch, briefing Branch, finding predecessor, and briefing predecessor substitutions are rejected. Projection now scopes by Company+Branch, binds finding digests, and fails closed on missing/conflicting nested authority. Latest/history ordering has a deterministic UUID tie-breaker. Fresh isolated r22 upgrades from zero to the single head with no drift; l0 downgrade/re-upgrade passes. The Luminary/LIA set passes 24/24, mutation registry 10/10, frontend 98 files/316 tests, ESLint/build, focused Ruff, MyPy, compilation, and diff gates are clean.
113. `LUMINARY_ANALYZE_READ_SOD`: The Luminary analysis endpoint required only `COMPANY_LUMINARY_ANALYZE` but returned the complete owner briefing and internally read admitted Economics evidence. A caller explicitly denied `COMPANY_LUMINARY_READ` could therefore retrieve protected intelligence through the mutation API. A reusable all-permissions dependency now requires both READ and ANALYZE for this response-bearing operation. A direct dependency attack proves ANALYZE alone receives a fixed 403 while the exact two-permission composition succeeds; read-only endpoints remain independently READ-gated. The focused Luminary/registry set passes 16/16 with production-source MyPy and Ruff clean.
114. `AUTHORIZATION_TEST_COLD_MODEL_REGISTRATION`: The authorization suite imported Jobs indirectly but not Scheduling, even though the Job mapper has a composite relationship to Appointment. It passed only after another test imported Scheduling and failed from a cold process with `NoReferencedTableError`. The fixture now explicitly registers Scheduling models. On fresh isolated r23, all 10 authorization tests pass cold after a zero-to-head upgrade; current=head and Alembic drift remain clean. The suite retains fixed fixture codes and is intentionally run once per isolated database rather than falsely claiming repeat-run cleanup.
115. `BUSINESS_EVENT/AUDIT_CROSS_TENANT_DURABLE_SCOPE`: Business Events, Audit records, delivery work, delivery evidence, consumer receipts, and consumer cursors copied Company/Branch/event/consumer scope in application code without database composition constraints. A repository or worker regression could therefore persist cross-Company audit truth or detach downstream evidence from its parent event. Migration `m1k0i59h6f2d` adds named Company+Branch, Company+Event, Event+Branch, Delivery+Event, Delivery+Branch, Delivery+Consumer, and cursor/receipt parent constraints plus Branch-requires-Company checks. Direct PostgreSQL attacks prove all six representative substitution layers fail at the intended constraint. Fresh r24 zero-to-head creates fully validated constraints, then downgrades/re-upgrades with one head and no drift. Existing r21 contains 17 pre-constraint synthetic events with nonexistent Branch IDs; immutable rows are preserved through a forward-enforcing `NOT VALID` event constraint rather than rewritten, while every new invalid write is rejected. Event fixtures now create real Company/Branch authority; the event/audit/security tranche passes 45/45 with focused MyPy, compilation, and model/migration lint clean. Event pagination also gains a UUID tie-breaker after occurred/created time.
116. `REQUEST_CORRELATION_EVENT/AUDIT_PROPAGATION`: The request middleware returned a safe correlation UUID, but default Business Event staging generated a new UUID and `AuditEntry` generated another at construction. One HTTP mutation therefore produced unrelated response, event, and audit identities unless each domain manually threaded correlation. Shared staging now preserves an explicit domain correlation first, otherwise adopts the active request correlation, and generates a fresh UUID only for background/non-request work. Tests prove one request identity reaches both Event and Audit, explicit identity is not overwritten, and background Audit identity remains distinct; correlation is not used as authorization or an idempotency key. The shared event/audit/security tranche passes 47/47 with focused Ruff and MyPy clean.
117. `BEACON_WORKFLOW_LIST_N_PLUS_ONE`: Both workflow-enriched Beacon list endpoints loaded current workflow state with one query per emitted signal. A new Company-scoped `DISTINCT ON` batch projection retrieves the highest workflow version for every requested condition in one query, rechecks Branch authorization for every returned row, and returns an identity map consumed by both routes. Filtering, escalation, and empty-workflow semantics are unchanged. A service test proves two conditions use one query; the HTTP boundary binds one batch invocation. The complete Beacon suite passes 134/134 with focused Ruff and MyPy clean. The adjacent Price Book aggregate remains an explicit owning-product pagination gate because partial service-item pages must reconcile versions, components, and option references without silent omission.
118. `PROTECTED_AUTHORITY_RECONCILIATION/STALE_FIXTURE`: Protected authority advanced to `429ac18bbbc381d3c6756599f7979d356d49e0f1`, absorbing the Luminary tenant-lineage tranche and adding Service Agreements, canonical launch-role provisioning, audit filtering, and owner-operations surfaces. The Alembic lineage remains linear: protected head `l0j9h48g5e1c` is the parent of OM2-C event/audit scope revision `m1k0i59h6f2d`. Combined qualification exposed two bootstrap assertions that still expected the pre-launch two-role model and an audit test that created an impossible cross-Company Branch pair now rejected by the durable constraint. The fixtures now assert all canonical launch roles plus the baseline role, remove every existing assignment before exercising catalog restoration, and construct valid same-tenant/foreign-tenant Branch evidence before testing read isolation. The reconciled focused backend tranche passes 204/204; fresh r25 upgrades from zero with one head/current=head/no drift; frontend passes 101 files/325 tests, repository ESLint, TypeScript, and production build; focused Ruff and Python compilation pass.
119. `RETENTION/DELETION_SAFETY_REVIEW`: A current application scan found no automatic hard-delete path for durable business, economic, payroll, migration, Event, or audit truth. Notification terminal cleanup archives records instead of deleting them; pay-statement experience explicitly defaults to `preserve`; LIA transcript retention remains an owner-policy gate and does not falsely claim durable storage; platform row deletion is limited to explicit Branch-access and role-assignment revocation; QBO token deletion occurs only during explicit disconnect after provider revocation, while other unlink operations remove temporary or recovery-control files rather than business evidence. No retention period was invented and no automatic deletion was enabled. The focused notification outbox, QBO source, pay-statement authority, and LIA suites pass 140/140 with one dependency deprecation warning.
120. `PROTECTED_AUTHORITY_RECONCILIATION/ECONOMICS_IMMUTABILITY`: Protected authority advanced to `a01469d67eb48a4d9cc9f6cd9cd4337fbe33425d`, integrating immutable Economics owner-intelligence history and previously qualified OM2-C Event/Audit, correlation, Luminary SOD, and Beacon batching work. Protected history inserts Economics revision `h6f8j0l2n497` between Luminary `l0j9h48g5e1c` and Event/Audit `m1k0i59h6f2d`; the merge preserves that linear ancestry and the later OM2-C evidence entries. Fresh isolated r26 upgrades from zero to one head with no drift, then downgrades both h6/m1 to l0 and re-upgrades cleanly. The combined Economics, Beacon, Event, Luminary, launch-control, and sensitive-output tranche passes 364/364; frontend passes 101 files/325 tests with repository ESLint, TypeScript, and production build.
121. `AUDIT/EVENT_BANK_DATA_CLASSIFIER_GAP`: The shared protected-output classifier rejected `bank_account_number` and `routing_number` but accepted common equivalent field names, allowing account or routing data under `account_number`, ABA/bank-routing variants, IBAN, or SWIFT/BIC keys to enter Audit details or Business Event payloads. The exact normalized aliases are now secret-classified without broadly rejecting arbitrary account-related prose. Nested adversarial payloads using punctuation and spacing variants fail before persistence, and the deterministic classifier fingerprint is updated to `bf261fe8ad8a819791a4a22fa122f7d0a6c148faf37111f51175faa4ddffb7af`. The combined 364-test security tranche passes with focused Ruff clean.
122. `PLAIN_LOG_SECRET_ASSIGNMENT_LEAKAGE`: The shared logging filter sanitized structured secret fields, bearer credentials, credential-bearing URLs, private keys, and exception chains, but an ordinary string log message containing assignment-shaped material such as `token=...`, `api_key: ...`, or a quoted client secret was emitted unchanged. A bounded case-insensitive text sanitizer now redacts only recognized token/key/password/secret assignment labels and their quoted or unquoted values while preserving adjacent operational status text. A direct handler test proves three distinct canaries are absent and the safe recovery status remains visible. The focused sensitive-output/security set passes 28/28 with Ruff clean.
123. `ECONOMICS_RESULT_HISTORY_CROSS_BRANCH_LINEAGE`: The integrated immutable Economics history compared Branch identity only when an active Branch was selected, and its durable supersession trigger omitted Branch from the predecessor/successor lineage tuple. A same-Company caller without active-Branch context could therefore link otherwise matching results from different Branches, while lineage reads could mix edges with the same subject/period across Branches. Application admission now requires both results to be in the caller's authorized Branch set, Branch identity is always part of the immutable lineage comparison, current-result queries scope to authorized Branches, and lineage edge/result reads bind back to the seed Branch and Company. Forward revision `n2l1j60i7g3e` replaces the authoritative validator without rewriting h6 history. A direct PostgreSQL attack using two valid cross-Branch results is rejected with a check violation; n2 downgrade/re-upgrade and drift pass, fresh r27 upgrades from zero to n2 with no drift, and the complete Economics suite passes 158/158 with focused Ruff and MyPy clean.
124. `PROTECTED_AUTHORITY_RECONCILIATION/LIA_SAFETY_FOUNDATION`: Protected authority advanced to `ab76f8f254974ce6b0e4c387fbe25335cb51e98e`, integrating a provider-neutral, read-only LIA foundation with explicit tool registry, context budgeting, evidence envelopes, prompt-injection resistance, structured-claim validation, golden evaluation cases, and authorized-Branch retrieval scope. No provider is configured or called and autonomous mutation remains disabled. Merge normalization initially changed a fingerprinted test file; canonical Ruff normalization restored its reviewed digest without weakening the qualification manifest. The combined LIA, output-control, launch-role, and bootstrap tranche passes 84/84 with focused Ruff clean; frontend remains 101 files/325 tests with ESLint, TypeScript, and production build clean; candidate schema remains the single n2 head.
125. `PAYROLL_PROTECTED_STORAGE_REPLAY/FAILURE_CONTRACT`: Payroll pay-statement generation already wrote protected custody before inserting database authority, but reporting-artifact replay returned an existing database row without re-reading or verifying its file; deleted or tampered custody could therefore be reported as successfully generated. Runtime filesystem errors from all statement/report render and retrieval endpoints also bypassed the explicit storage-unavailable classification. Reporting replay and retrieval now both verify digest and byte size, and artifact endpoint `OSError` paths return fixed non-reflective HTTP 503 `dependency_unavailable`/`OWNER_ADMIN_ACTION_REQUIRED` responses after domain authorization/conflict types are resolved. A TEST-only failing storage adapter proves a write failure creates no additional artifact authority. The complete Payroll suite passes 109/109 with focused Ruff and MyPy clean.
126. `PROVIDER_UNCERTAINTY_CROSS_DOMAIN_QUALIFICATION`: A synthetic provider-boundary tranche confirms the shared state model distinguishes pre-acceptance transport failure from accepted, rejected, uncertain, and acknowledged authority. `UNCERTAIN` selects reconcile-before-retry rather than blind replay; Payments possible acceptance creates reconciliation authority without duplicate receipt/economic truth; Payroll payment execution and remittance reject resubmission while uncertain and reject contradictory acknowledgements; notification delivery expiration after submission becomes durable ambiguous evidence; QBO OAuth/token adapter tests use only synthetic transports and custody. The combined generic reliability, notification, Payments, Payroll provider, and QBO adapter set passes 83/83 with one dependency deprecation warning. No external provider was contacted.
127. `PROTECTED_AUTHORITY_RECONCILIATION/OWNER_INTELLIGENCE_COMPOSITION`: Protected authority advanced to `a976fa1a18246f546140faf288e094cd10f9ddd0`, absorbing the independently qualified Economics Branch-lineage and protected-output classifier work and adding governed LIA retrieval, Economics source-completeness, and LIA/Luminary frontend composition. Merge resolution preserves the expanded evidence-reference contract and all prior OM2-C qualification entries. The protected foundation manifest retained the pre-normalization digest for its fingerprinted safety test; the manifest now binds the reconciled file digest and recomputed deterministic fingerprint `9ae7860b749e81d14289014475f2694543dc70dda6dfce4805d9c19a03958f65`. The combined Economics, LIA, and output-control tranche passes 229/229; frontend passes 101 files/326 tests with repository ESLint, TypeScript, and production build; PostgreSQL remains at exactly one `n2l1j60i7g3e` head with current=head and no drift.
128. `ECONOMICS_WORKSPACE_CROSS_BRANCH_READ_SCOPE`: Economics immutable-history admission was Branch-safe, but the owner workspace scoped result detail and overview records only when an active Branch was selected. A same-Company user with a restricted authorized-Branch set and no active Branch could therefore retrieve a valid result from another Branch by UUID or include it in overview projections; nested Job identity enrichment was likewise Company-only. Detail and overview now admit only Company-level results or results in the caller's authorized Branch set, Job enrichment binds Company plus authorized Branch, and predecessor/successor edge reads explicitly bind Company. An adversarial query-shape test proves the no-active-Branch detail boundary contains the authorized-Branch predicate and conceals a nonmatching identity. The 229-test combined tranche, focused Ruff, and production-source MyPy pass.
129. `LIA_CONTEXT_OBJECT_BINDING/OVERBROAD_RETRIEVAL`: LIA propagated a contextual entity UUID to generic and Luminary adapters, but its Economics, Beacon, Migration, and Payroll adapters silently ignored that identity and returned broad authorized collections. A resource-specific question could therefore disclose unrelated protected metadata even though Company, Branch, and permission scope remained valid. Every special adapter now receives the contextual identity: persisted Economics result, Migration run, Payroll reporting snapshot, and own pay-statement queries bind the exact UUID; Beacon filters the already-authorized queue to that signal; and own statement retrieval continues to resolve Employee authority from the authenticated Membership before applying the statement identity. Adversarial tests bind propagation, SQL object predicates, Beacon exclusion, and Membership→Employee→statement composition. The combined LIA, Beacon, Economics, Payroll, and Operational Migration tranche passes 654 tests with 7 environment/fixture skips and two pre-existing HTTP-constant deprecation warnings; focused Ruff and production-source MyPy pass.
130. `LIA_PAYROLL_READINESS/PRIVACY_BUDGET_DRIFT`: The live LIA foundation registry and qualification artifact still classified own-pay context as blocked pending Membership→Employee resolution even though the integrated adapter now resolves that identity server-side and enforces own-record scope. Runtime also returned metadata for up to 50 issued statements despite the fingerprinted source contract's `max_results=1`. Own-pay retrieval now admits only the latest issued statement metadata, never accepts an Employee identity from the client/model, and retains exact contextual statement binding. The source registry and readiness documentation truthfully classify this bounded adapter READY; the obsolete external gate is removed while provider, retention, Customer projection, Preview acceptance, and Production-mutation gates remain. Fingerprinted evidence now binds the runtime registry, one-result test, and readiness documentation at `8aad8cf644a4d2ea9192f83d7ef99cc4e61f762d803f7d1438c845422dcb40f9`. The focused LIA/pay-statement tranche passes 59/59 with Ruff, MyPy, JSON, and diff gates clean.
131. `LIA_SOURCE_REGISTRY_COMPLETENESS/ECONOMICS_BUDGET`: The governed runtime exposed Estimates, Payments, Purchasing, Inventory, Luminary, Migration, and Payroll reporting adapters without corresponding explicit source-registry entries, so `/foundation-readiness` did not enumerate the permissions, sensitivity, Branch behavior, result bounds, freshness, or provenance for every callable evidence source. Economics also loaded up to 100 admitted results despite its registered 20-result maximum. The registry now declares all 14 runtime bindings, including distinct Payroll reporting and own-statement authorities, and a cross-module invariant proves every runtime source ID exists with the exact permission used before query execution. Economics enforces and tests the 20-result ceiling. The readiness documentation and deterministic manifest bind the complete inventory at fingerprint `4432898ca3df2ab8af03c933907f808c74f7b437e959788e83b1636f7540bd4a`; no permission or new runtime source was added.
132. `OPERATIONAL_MIGRATION_REPEAT_RUN_TEST_ISOLATION`: The integrated evidence-domain suite passed once on a clean database but two Operational Migration dry-run tests asserted global table emptiness or globally unique synthetic source identity. On repeat qualification against the supported persistent test database, valid rows from earlier synthetic Companies caused false failures. All affected pre/post-import assertions now bind the test's generated Company, including source identities, financial rows, line items, and payment lookup. Both formerly failing tests pass twice consecutively against the populated database. The complete LIA, Beacon, Economics, Payroll, and Operational Migration tranche then passes 656 tests with 7 intentional skips and two pre-existing HTTP-constant deprecation warnings; focused Ruff, MyPy, compilation, JSON, deterministic fingerprint, and diff gates are clean.
133. `LIA_PROTECTED_SOURCE_PERMISSION_COMPOSITION`: A direct negative permission matrix now proves protected LIA sources do not admit access by permission-name or domain affinity. Migration rehearsal execute does not imply Migration readiness read; Luminary analyze does not imply Luminary read; Beacon review/own/assign do not imply Analytics read; Economics policy manage does not imply measurement read; Payroll reporting manage/approve and statement administrative read/manage do not imply either reporting read or own-statement read. The two explicit Payroll read permissions independently admit the Payroll domain. This preserves `Migration read ≠ execute`, `Luminary read ≠ analyze`, Beacon workflow ≠ signal read, and Payroll reporting/own/admin separation at the API retrieval boundary. The combined LIA, canonical launch-role, authorization-matrix, and authorization-service tranche passes 104/104 with focused Ruff clean; no permission catalog or product authority changed.
134. `PROTECTED_API_CACHE_CONTROL_OVERRIDE`: The shared security middleware defaulted API responses to `private, no-store` only when a handler supplied no cache directive. A protected handler could therefore emit `no-cache`, `public`, or positive `max-age`, allowing storage or explicitly shared caching despite the application-wide boundary. Every `/api/*` response now receives an authoritative `private, no-store` policy regardless of the handler's original caching directive; `no-transform` is preserved when required for artifact fidelity. Boundary tests prove absent, revalidation-only, and deliberately unsafe public/max-age headers all fail closed while the non-API root remains unaffected. The focused enterprise-security, correlation/reliability, and sensitive-output tranche passes 30/30 with one dependency deprecation warning; Ruff and production-source MyPy pass.
135. `PLATFORM_AUTHORIZATION_REPEAT_RUN_IDENTITIES`: The full Platform rerun exposed ten authorization-service tests whose shared fixture used fixed globally unique Company, Branch, Permission, Role, and User identities. They passed only on a clean database and then failed before exercising authorization semantics. The fixture now generates a per-call persisted identity namespace and returns the exact Company/Branch/Permission/Role codes to assertions and router dependencies. All 10 DB-backed tests pass twice consecutively against the populated qualification database. A subsequent Platform-minus-Redis run passes 181 tests and identifies 22 additional pre-existing deterministic persistence/service fixtures for continued `TEST_ISOLATION` repair; the Redis-dependent rate-limit test remains `PENDING_INTEGRATED_REDIS_QUALIFICATION` and is not claimed passed.
136. `PLATFORM_PERSISTENCE_REPEAT_RUN_ISOLATION_CLOSURE`: The remaining 22 Platform failures were all deterministic-fixture collisions across authentication persistence/services, Company/Branch constraints, identity persistence, permission persistence, and one unknown-permission administration test. Fixed globally unique emails, Company/Branch codes, Permission codes, and token hashes now receive a per-test synthetic namespace, while each duplicate/rotation/constraint attack deliberately reuses the generated identity within its test. No existing evidence is deleted and no database uniqueness invariant is weakened. Authentication persistence passes 7 tests twice; authentication services passes 6 tests twice with the Redis case excluded; identity/permission persistence passes 11 tests twice; Company/administration passes 17 tests twice. The complete Platform suite then passes 203 tests with only the one explicit Redis-dependent rate-limit test deselected and one dependency deprecation warning. Focused Ruff and diff gates are clean; Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.
137. `BACKEND_TEST_COLLECTION_NAMESPACE_COLLISION`: The broad backend suite could not collect because Accounting and Luminary each supplied a top-level `test_api_boundary.py` while their test directories were not Python packages, causing pytest's import-mismatch protection to bind both paths to one module name. Explicit package markers give both domains stable, distinct module identities without changing product behavior or test selection. Collection now discovers 2,375 tests with only the named Redis-dependent case excluded from execution.
138. `BROAD_BACKEND_REPEAT_QUALIFICATION`: Persistent-database qualification exposed three test-isolation defects rather than product regressions: an Invoice test counted every historical line and AR entry instead of the invoice under test; a QBO callback assertion retained the pre-hardening cache directive; and Jobs teardown attempted to delete Branch authority now durably referenced by immutable Business Events. Assertions now bind their generated Invoice, the QBO contract expects the authoritative `private, no-store` boundary, and Jobs preserves its synthetic evidence rather than deleting referenced authority. The repaired focused tranche passes 61 tests. The subsequent broad backend run passes 2,367 tests with 7 intentional skips and only the explicit Redis rate-limit case deselected; 39 warnings are dependency deprecations or existing SQLAlchemy transaction warnings, not failures. Alembic reports one head (`n2l1j60i7g3e`), current=head, and no schema drift; focused Ruff and diff gates are clean. Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.
139. `FRONTEND_FULL_REGRESSION_REPEAT_GATE`: The current integrated frontend passes all 101 Vitest files / 326 tests, repository-wide ESLint, TypeScript project compilation, and the production Vite build under healthy host pressure. The build emits 2,640 transformed modules and completes without chunk-size, type, lint, or test failures. This is a NON-PRODUCTION repository qualification only; Preview acceptance and Enterprise release authority remain external gates.
140. `SECRET_SIGNATURE_SCAN/ENGINEERING_TEST_ISOLATION`: A filename-only tracked-source scan found no GitHub token, AWS access-key, live payment-key, PGP private-key, or deployable private-key artifact signature. Four private-key marker matches are intentional rejection/redaction canaries in Development Factory, Engineering Control, LIA, and Platform safety tests; only example environment templates are tracked. This bounded signature scan does not claim arbitrary text or repository history is secret-free. Focused execution then exposed Engineering command tests' hidden dependence on prior mapper imports: the suite loaded Appointment transitively without registering its Customer relationship target. The test module now explicitly registers Customer models and passes 23 tests twice in isolation; the combined canary/redaction tranche passes 95 tests with one dependency deprecation warning. Focused Ruff and diff gates are clean.
141. `DEPENDENCY_ADVISORY_REMEDIATION`: Registry-backed audits found four fixable transitive frontend development findings and ten Python advisory records across the installed `cryptography 45.0.7` and `pytest 8.4.2` dependency set (including duplicated cryptography records from the nested requirements input). The frontend lock now advances only the affected transitive packages within existing dependency ranges: `brace-expansion 5.0.9`, `nanoid 3.3.18`, `postcss 8.5.26`, and `undici 7.29.0`. Python bounds now require the controlled compatible set `cryptography>=50,<51`, `pytest>=9.0.3,<10`, and the coupled `pytest-asyncio>=1,<2`; the resolved qualification environment uses 50.0.1, 9.1.1, and 1.4.0 with `pip check` clean. Both frontend `npm audit` and the 50-package Python `pip-audit` return zero findings. Frontend `npm ci`, 101 files / 326 tests, ESLint, TypeScript, and production build pass. All 2,375 backend tests collect on pytest 9; 54 AES-GCM/Ed25519/onboarding boundary tests and the broad 2,367-test non-Redis suite pass. Production-source MyPy and compilation pass. Repository-wide Ruff still reports 25 pre-existing style/static findings outside this change; changed-test Ruff and diff gates are clean and no mass formatting was performed.
142. `PROCUREMENT_PERFORMANCE_UTC_BOUNDARY_ISOLATION`: The dependency acceptance run crossed midnight UTC and exposed a deterministic Procurement test defect: vendor-performance evidence used a hard-coded August 31 midnight cutoff while the synthetic Purchase Order used the live database creation clock. Evidence created after that cutoff was correctly excluded, making the test dependent on execution time. The as-of authority now derives from the generated order's persisted timestamp plus one day, retaining the intended historical projection without a wall-clock race. The focused test passes twice after the UTC boundary and the subsequent broad backend run passes 2,367 tests with 7 intentional skips and only the Redis-dependent case deselected.
143. `CONTROLLED_EXECUTION_OFFER_COMPANY_BINDING`: Controlled execution offers bound Company to the execution but stored command, acquired lease, worker, and transport-session UUIDs without tenant-composite foreign keys. Application acquisition normally composed coherent objects, yet durable storage permitted valid foreign-Company identities and the worker HTTP response dereferenced the acquired lease by UUID alone. Offers now bind all five authoritative identities through `(company_id, id)` foreign keys, replacing the global-only command reference; the HTTP response also reloads its lease with Company plus UUID. The populated qualification database had zero existing mismatches. Transactional attacks substituting valid foreign-Company command, lease, worker, and session identities are each rejected by PostgreSQL and rolled back. Migration `o3m2n71j8h4f` passes populated upgrade, downgrade/re-upgrade, and a zero-to-head upgrade on fresh r28 with one head, current=head, and drift clean. The 41-test controlled-execution/worker-HTTP regression, focused Ruff, MyPy, compilation, and diff gates pass.
144. `CONTROLLED_RESULT_EXACT_PARENT_BINDING`: Controlled execution results referenced their parent offer by Company and offer UUID but duplicated command, execution, lease, worker, and transport-session identities without requiring those fields to equal the parent. A result could therefore remain tenant-scoped yet contradict the exact execution authority it purported to evidence. The offer now publishes one unique result-binding tuple and the result's foreign key covers the complete tuple, making every duplicated identity parent-authoritative. The populated database had zero existing contradictions. Transactional attacks substituting valid contradictory command, execution, lease, worker, or session UUIDs are each rejected and rolled back. Migration `p4n3o82k9i5g` passes populated and fresh-r28 upgrade, downgrade/re-upgrade, current=head, and drift gates. The expanded 43-test controlled-execution/worker-HTTP regression, focused Ruff, MyPy, compilation, and diff gates pass.
145. `WORKER_RECOVERY_EXACT_OFFER_LINEAGE`: Worker recovery acknowledgement creation validated Company, command, execution, offer, lease, and worker composition in application code, but storage used global-only command/offer FKs plus independent tenant-scoped execution/lease/worker FKs. Durable evidence could therefore be mutated into a contradictory same- or cross-tenant composition after validation. The offer now publishes a recovery-lineage tuple and each acknowledgement references the exact Company/offer/command/execution/lease/worker tuple. The populated database had zero contradictions. A transaction inserted one coherent synthetic acknowledgement, then proved valid contradictory substitutions of offer, command, execution, lease, and worker UUIDs are each rejected before rolling back. Migration `q5o4p93l0j6h` passes populated and fresh-r28 upgrade, downgrade/re-upgrade, current=head, and drift gates. The combined 51-test recovery/controlled-execution/worker-HTTP regression, focused Ruff, MyPy, compilation, and diff gates pass.
146. `CONTROLLED_LINEAGE_BROAD_ACCEPTANCE`: After the controlled-offer Company binding, exact result-parent binding, and recovery acknowledgement lineage revisions, the full populated-database backend suite passes 2,372 tests with 7 intentional environment/fixture skips and only the explicit Redis-dependent rate-limit case deselected. The 39 warnings remain dependency deprecations or existing SQLAlchemy transaction warnings. This broad pass ran at the single current schema head `q5o4p93l0j6h` on Python 3.12 with the remediated cryptography/pytest dependency set; no indirect ORM, fixture, migration, or cross-domain regression appeared. Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.
147. `SERVICE_AGREEMENT_ROOT_TENANT_BINDING`: Service Agreement enrollment and renewal queried Customer, Plan, and predecessor authority by Company in application code, but durable storage used global-only UUID foreign keys and neither Plan nor Agreement exposed a `(company_id, id)` tenant identity. A row could therefore be mutated into a foreign-Company Customer, Plan, or renewal predecessor after validation. Plans and Agreements now publish tenant identities, and all three Agreement references are Company-composite foreign keys. A transaction created two coherent synthetic Company agreements, proved valid cross-Company Customer, Plan, and predecessor substitutions are each rejected, and rolled back all evidence. Migration `r6p5q04m1k7i` passes populated and fresh-r28 upgrade, downgrade/re-upgrade, current=head, and drift gates. The Service Agreement contract suite passes 5/5; focused Ruff, MyPy, compilation, and diff gates pass. Child agreement evidence/coverage tenant parity remains under active audit.
148. `SERVICE_AGREEMENT_CHILD_PARENT/TENANT_BINDING`: Agreement coverage, entitlements, lifecycle evidence, and billing occurrences carried Company/Branch authority but referenced Agreements globally; optional Appointment, Job, and Invoice identities had no database FK, and lifecycle evidence did not require its Entitlement to belong to the same Agreement. Child rows now bind to Agreement Company/Branch, Entitlements bind optional operational objects to the same Company/Branch, lifecycle evidence binds the exact Agreement/Entitlement tuple, and billing binds any Invoice to Company. Because Service Location derives tenant from Customer rather than duplicating Company, a shared trigger requires coverage and entitlement locations to belong to the Agreement's exact Customer. A coherent two-Company synthetic composition inserted successfully; nine attacks using foreign or contradictory Agreement, Location, Branch, Appointment, Job, Entitlement, and Invoice identities were rejected before the transaction rolled back. Migration `s7q6r15n2l8j` passes populated and fresh-r28 upgrade, downgrade/re-upgrade, current=head, and drift gates. The composed Customers/Scheduling/Jobs/Invoicing/Service Agreement regression passes 295 tests with four existing SQLAlchemy transaction warnings; focused Ruff, MyPy, compilation, and diff gates pass.
149. `SERVICE_AGREEMENT_BINDING_BROAD_ACCEPTANCE`: The complete populated-database backend suite at schema head `s7q6r15n2l8j` passes 2,374 tests with 7 intentional environment/fixture skips and only the explicit Redis-dependent rate-limit case deselected. All Agreement root/child constraints and the exact Customer→Location trigger remain active through cross-domain Customer, Scheduling, Job, Invoice, migration, and lifecycle paths. The 39 warnings remain dependency deprecations or existing SQLAlchemy transaction warnings; no indirect ORM, fixture, schema, or domain regression appeared. Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.
150. `FIELD_SERVICE_EXACT_EVIDENCE_LINEAGE`: Technician notes, customer approvals, invoice handoffs, completion snapshots, and non-billable dispositions carried Company/Branch/Job authority but referenced Dispatch assignments by globally valid UUID alone. Completion evidence likewise referenced its snapshot globally, and invoice handoff could reference an Invoice whose tenant or operational Job contradicted the handoff. The service always derives these identities from one exact assignment, snapshot, or Company/Branch/Job-scoped Invoice, so durable storage now enforces those existing semantics through exact `(company_id, branch_id, job_id, id)` parent bindings. The populated database had zero existing assignment, snapshot, or invoice contradictions. Migration `t8r7s26o3m9k` passes populated and fresh-r28 upgrade, fresh downgrade/re-upgrade, current=head, and drift gates. Metadata tests bind every exact foreign-key contract; the composed Dispatch/Invoicing/Field Service regression passes 38 tests and the Field Service suite passes 11 tests against PostgreSQL. Focused Ruff, MyPy, compilation, and diff gates pass. The repository's Docker-only `postgres` hostname caused one initial environment-only invocation failure; explicit isolated `DATABASE_URL` qualification passed and no shared database was changed.
151. `FIELD_SERVICE_LINEAGE_BROAD_ACCEPTANCE`: The complete populated-database backend suite at schema head `t8r7s26o3m9k` passes 2,375 tests with 7 intentional environment/fixture skips and only the explicit Redis-dependent rate-limit case deselected. The new Dispatch assignment and Invoice parent keys remain compatible across all ORM metadata creation, Field Service evidence, Invoicing, migration, and cross-domain paths. The 39 warnings remain dependency deprecations or existing SQLAlchemy transaction warnings; no indirect schema, fixture, lifecycle, authorization, or application regression appeared. Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.
152. `INVOICE_AR_EXACT_TENANT/REPLAY_LINEAGE`: Invoice Customer, Invoice idempotency replay, AR Customer, and payment-receipt Customer references accepted globally valid UUIDs even though each record carries Company authority. AR additionally bound Invoice by Company while allowing its independent Branch and Customer fields to contradict that Invoice. Application creation composed coherent rows, but post-validation durable mutation could cross tenant boundaries or fork economic lineage. Invoice and receipt Customer references now bind `(company_id, customer_id)`; idempotency binds `(company_id, invoice_id)`; and AR binds the Invoice's exact `(company_id, branch_id, invoice_id, customer_id)` payment scope. The populated database had zero existing contradictions in all four relationships. Migration `u9s8t37p4n0l` passes populated and fresh-r28 upgrade, fresh downgrade/re-upgrade, current=head, and drift gates. The composed Invoicing/Payments/Accounting/Field Service regression passes 76 tests with six existing SQLAlchemy transaction warnings; focused Ruff, MyPy, compilation, and diff gates pass.
153. `ESTIMATE_CUSTOMER/LOCATION_AUTHORITY`: Both the legacy migration Estimate and authoritative Estimate Proposal stored Company authority while referencing Customer by globally valid UUID. The legacy Estimate also referenced Customer and Service Location independently, allowing a valid Location belonging to another Customer. Proposal application code already required exact location/customer composition, but neither Estimate root durably bound Customer to Company. Both roots now use `(company_id, customer_id)` foreign keys and legacy Estimate additionally binds `(service_location_id, customer_id)`. Populated-data scans found zero Company or location/customer contradictions. Migration `v0t9u48q5o1m` passes populated and fresh-r28 upgrade, fresh downgrade/re-upgrade, current=head, and drift gates. The composed Estimate/Customer Migration/Operational Migration/Jobs/Invoicing regression passes 597 tests with 7 intentional skips and 17 existing SQLAlchemy transaction warnings; focused Ruff, MyPy, compilation, and diff gates pass.
154. `JOB_CUSTOMER/LOCATION_AUTHORITY`: Jobs durably bound Company/Branch but referenced Customer and Service Location as independent global UUIDs. Application creation rejected invalid composition, yet persistence allowed a foreign-Company Customer or a Location belonging to another Customer, contaminating Scheduling, Dispatch, Field Service, Estimate conversion, and Invoice lineage downstream. Jobs now bind Customer through `(company_id, customer_id)` and Location through the exact `(service_location_id, customer_id)` pair. Populated-data scans found zero existing Company or location/customer contradictions. Migration `w1u0v59r6p2n` passes populated and fresh-r28 upgrade, fresh downgrade/re-upgrade, current=head, and drift gates. All 192 Jobs tests and the 127-test Scheduling/Dispatch/Field Service/Invoicing/Estimate boundary pass; the latter retains 23 existing SQLAlchemy transaction warnings. Focused Ruff, MyPy, compilation, and diff gates pass.
155. `CUSTOMER_MIGRATION_RUN/SOURCE_IDENTITY_LINEAGE`: Customer Migration runs stored Company and Branch independently, Customer source identities independently referenced Branch, Customer, and first run, and contact/location source identities referenced first run globally. These gaps permitted durable cross-Company or cross-Branch replay lineage despite service-level composition. Runs now bind `(company_id, branch_id)` and publish Company/run plus Company/Branch/run identities; Customer source identities bind exact Branch, Customer tenant, and Company/Branch first run; contact and location identities bind first run to Company while preserving older nullable location-Branch semantics. Populated-data scans found zero run-Branch, Customer-source, contact-run, or location-run contradictions. Migration `x2v1w60s7q3o` passes populated and fresh-r28 upgrade, fresh downgrade/re-upgrade, current=head, and drift gates. The combined Customer and Operational Migration suite passes 359 tests with 7 intentional skips; focused Ruff, MyPy, compilation, and diff gates pass.
156. `CUSTOMER_ECONOMIC/MIGRATION_LINEAGE_BROAD_ACCEPTANCE`: After exact Customer authority was added across Estimates, Jobs, Invoices/AR, Field Service evidence, and Customer Migration source identity, the complete populated-database backend suite passes 2,375 tests with 7 intentional environment/fixture skips and only the explicit Redis-dependent rate-limit case deselected. This pass ran at single schema head `x2v1w60s7q3o`; all new parent keys and composite foreign keys remained compatible across ORM metadata, migrations, cross-domain lifecycle workflows, and repeat-run fixtures. The 39 warnings remain dependency deprecations or existing SQLAlchemy transaction warnings. Redis remains `PENDING_INTEGRATED_REDIS_QUALIFICATION`.

## Release-readiness projection

- Release SHA: protected authority `a976fa1a18246f546140faf288e094cd10f9ddd0` plus the OM2-C qualification commit recorded on this branch.
- Schema head: candidate Customer Migration lineage revision `x2v1w60s7q3o` over Job customer-authority `w1u0v59r6p2n`, Estimate customer-authority `v0t9u48q5o1m`, Invoice/AR exact-lineage `u9s8t37p4n0l`, Field Service exact-lineage `t8r7s26o3m9k`, Service Agreement child-binding `s7q6r15n2l8j`, Agreement root tenant binding `r6p5q04m1k7i`, worker-recovery lineage `q5o4p93l0j6h`, controlled-result parent binding `p4n3o82k9i5g`, controlled-offer Company binding `o3m2n71j8h4f`, Economics Branch-lineage revision `n2l1j60i7g3e`, Event/Audit revision `m1k0i59h6f2d`, immutable Economics revision `h6f8j0l2n497`, and Luminary revision `l0j9h48g5e1c`; fresh-upgrade ancestry, focused downgrade/re-upgrade, and drift clean.
- Backend: broad functional coverage is high, but the integrated Redis test and a clean one-pass fresh-database rerun remain external gates.
- Frontend: tests/lint/build qualified again after the bounded dependency repair.
- Security: focused authorization/isolation/idempotency/safe-output suite qualified; current frontend and Python dependency audits are clear. Mobile's 18 advisory findings require an Expo 54→57 SDK-major remediation owned by the Mobile lane, and expanded direct API object-binding remains open work.
- Known P0/P1 defects discovered in this tranche: none.
- Projection: **NON_PRODUCTION_CONDITIONALLY_QUALIFIED**. Enterprise retains deployment and release authority.

## Reproduction methodology

Host pressure was checked before heavyweight work. Tests used Python 3.12 and an isolated local PostgreSQL 16 database created only for OM2-C. The standard container hostname `postgres` was replaced through `DATABASE_URL`; no shared lane database was modified. Broad backend invocation required `ENVIRONMENT=test PYTHONPATH=.`. No Production load, provider calls, real journals, real payments, or real data were used.
