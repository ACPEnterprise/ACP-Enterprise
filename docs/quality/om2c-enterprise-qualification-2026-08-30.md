# OM2-C Enterprise qualification evidence — 2026-08-30

## Authority and scope

- Starting protected authority: `72f969bac74a0bf4c741bdc98133b5c23f18d91e` (`origin/customer-management-v1`).
- Reconciled protected authority: `1fac637462b01869cc7962d7463af1dc12637a43`.
- Latest reconciled protected authority: `f0fb25e4766abbbeda9b73e66c5d1f9bdf89d0a5`.
- Qualification branch: `work/om2c-quality-security-program-1`.
- The supplied OM2-A and OM2-B SHAs are not ancestors of starting authority. Their named remote branches were absent after `git fetch --prune`; no integration claim is made.
- This is non-production evidence. It is not Production release approval.

## Risk-weighted coverage map

| Domain | Current evidence | Highest-priority gap / limitation |
| --- | --- | --- |
| Platform / Auth / AuthZ | Strong unit, API, persistence, isolation, idempotency, provider reliability, security-output, header and permission-matrix suites (23 files) | Redis-backed authentication qualification requires an integrated Redis runtime; broad DB fixtures are not self-cleaning |
| Customers | Strong service/API/search/lifecycle/normalization coverage (10 files) | Cross-domain Customer→Location→Job object-binding rehearsal remains integration-level |
| Estimates | Pricing, workflow, conversion, normalized conversion replay, exactly-one Job/event replay, and list API coverage (5 files) | Deterministic multi-connection acceptance/conversion stress is blocked by the transaction-bound shared-connection fixture and remains a dedicated-suite gap |
| Jobs | API, permission, persistence, query, lifecycle service, deterministic lifecycle winner/event, and safe recovery-envelope coverage (5 files) | Broader cross-workflow Job/Appointment/Dispatch competing-transition stress remains incomplete |
| Scheduling / Dispatch | API, persistence, service, assignment serialization, digest-bound replay, contradiction rejection, and reschedule coverage | Large projection performance baselines and true multi-connection reschedule stress remain limited |
| Field Service | Contract, conformance and migration coverage (3 files) | Provider/storage failure composition is integration-only |
| Inventory / Purchasing | Foundation, adjustments, reservations, cycle counts, purchasing policy, replenishment, three-way match, current-authority replay enforcement, and deterministic concurrent replay coverage for movements/reservations/adjustments/cycle counts | Receipt and three-way-match concurrent-winner stress remains incomplete at current authority |
| Price Book | API and service coverage (2 files) | Scale/search and cross-tenant nested-ID abuse need expansion |
| Invoicing / Payments / AP | Contract, provider, AR/AP invariant, recovery-envelope, contradictory replay, and multi-connection collection/refund/disbursement-application exactly-once coverage | Deposit/settlement concurrency and cross-domain double-count rehearsal need broader end-to-end evidence |
| Accounting | Posting, API, contract, migration, balanced persistence, concurrent create/post/reversal, exactly-one Event, and safe recovery coverage | Full synthetic AR/AP/Payroll settlement balance rehearsal remains integration-level |
| Payroll | Strong policy, calculation, approval/finalization, payment, remittance, reporting and protected artifact coverage (23 files) | Redis/storage integrated failure injection and payment-provider restart replay remain pending |
| Migration | Extensive Customer and operational migration evidence (46 files) | Protected external source/storage rehearsal remains environment-dependent; no real provider contacted |
| Business Economics | Policy, admission, allocation, profitability, source-conformance, owner-workspace and explicit Company/Branch persistence enforcement | Cross-source economic double-counting still needs a consolidated rehearsal |
| Beacon | Evaluation, lifecycle, workflow, prioritization and acknowledgement coverage (16 files) | Read-only versus operational mutation matrix should be expanded at API level |
| Mobile-facing APIs | Backend employee/engineering APIs and separate mobile Jest suite exist | Native mobile was not changed in this tranche; integrated device accessibility remains external |
| Administration / frontend | Route/component tests cover authorization and representative loading/error states | Automated accessibility tooling and responsive viewport regression are not comprehensive |

## Qualification results

- Frontend after authority reconciliation and repairs: 88 test files / 263 tests passed; ESLint passed; TypeScript and production Vite build passed.
- Backend focused security tranche: isolation, authorization composition, idempotency, provider uncertainty and safe-output tests passed after repairing a stale catalog fingerprint.
- Backend broad run: 2,109 passed, 7 skipped, 12 failed. Classification: 10 `TEST_ISOLATION` failures caused by reusing a database containing focused-test fixtures (all 10 passed on a fresh schema); 1 `ENVIRONMENT` Redis test (failed closed because `redis` hostname/runtime was unavailable); 1 `STALE_FIXTURE` external-adoption test repaired and requalified.
- Static: Python 3.12 compilation passed; MyPy passed across 621 source files. Repository-wide Ruff is not a clean gate at starting authority (246 findings, predominantly pre-existing import-order findings); no mass formatting was performed.
- Database: PostgreSQL 16.15 fresh upgrade passed, followed by successful authority-advance upgrades; exactly one candidate Alembic head `f4d3b82a9e5c`; `current=head`; `alembic check` reports no drift. OM2-C migrations passed focused downgrade/re-upgrade qualification, and the Supply Chain/OM2-C merge passed a second entirely fresh database upgrade.
- Secret scan: only intentional synthetic canary tests matched the high-confidence token/private-key patterns; no discovered secret value was emitted.

## Defects and repairs

1. `STALE_FIXTURE`: sensitive-output catalog expansion changed the deterministic fingerprint without updating its qualification assertion. The assertion now binds the expanded catalog.
2. `STALE_FIXTURE`: external-adoption successor evidence did not satisfy newly integrated execution-head and repository-readiness admission. The test now supplies matching authoritative-head evidence and isolates the orthogonal readiness adapter with an explicit mock.
3. `DEPENDENCY`: direct `react-router` 7.18.1 fell within the lockfile audit's affected CSRF-bypass range. It was advanced only to the patched 7.18.3 line. Remaining audit findings are transitive and require separate applicability/upgrade qualification.
4. `AUTHORIZATION/DURABLE_INTEGRITY`: Economics execution checked Company identity but accepted a result tagged to a different or nonexistent Branch, and the durable result table lacked a Company+Branch foreign key. Persistence now rejects active-branch escape and nonexistent Company branches before mutation; migration `c1a0e38d6bfc` enforces the same invariant in PostgreSQL. All 140 Business Economics tests pass, and the new migration passed fresh upgrade, downgrade/re-upgrade, current-head, and drift qualification.
5. `FRONTEND_AUTHORIZATION/RECOVERY`: the Economics route initiated its protected workspace request even when the current user lacked read authority, and its temporary-unavailable state had no recovery action. The query is now disabled unless read permission is present, and the safe error state exposes a tested retry without rendering backend details.
6. `OBJECT_AUTHORIZATION/IDEMPOTENCY`: Purchasing resolved PO-scoped durable receipts before checking the caller's current Branch authority. Possession of an exact key and request could therefore replay a Branch A PO mutation result to a same-Company caller restricted to Branch B. The shared Purchasing replay boundary now reauthorizes the parent PO before receipt lookup for update, line, lifecycle, receipt, discrepancy, return, disposition, and change operations. The adversarial cross-branch replay fails closed, and 76 Purchasing/Procurement/Inventory tests pass.
7. `CONCURRENCY/IDEMPOTENCY`: Inventory movement, reservation, adjustment, cycle-session and cycle-entry creation used read-before-insert replay checks without serializing same-key transactions. Concurrent exact replay could expose raw unique-constraint failures, and adjustment replay could create a movement before losing the adjustment race. Each Company+operation+normalized-key now takes a transaction-scoped PostgreSQL advisory lock before authoritative lookup. Deterministic two-transaction tests prove one returned identity and one durable record; all 28 Inventory tests, focused Ruff, MyPy, and diff checks pass.
8. `CONCURRENCY/IDEMPOTENCY`: The shared Purchasing replay boundary performed a read-before-insert lookup without serializing Company+key. PO mutations happened to gain protection from parent-row locks, but create-style commands could race into the command-receipt unique constraint; the Vendor test labeled concurrent only exercised sequential replay. The shared boundary now takes a transaction-scoped PostgreSQL advisory lock after any required current-authority check and before receipt lookup. True concurrent Vendor replay returns one authority, competing receipt commands retain one typed winner, and 80 Purchasing/procurement-matching/Inventory tests pass.
9. `IDEMPOTENCY`: Estimate conversion validated and persisted a trimmed idempotency key but compared replay evidence to the untrimmed request. A whitespace-bearing key therefore succeeded once and falsely conflicted on exact replay. Conversion now normalizes once before validation, comparison, and persistence; replay produces one Job, one conversion, and one Business Event. All 32 Estimate tests pass. The existing Estimate fixture binds sessions to one transaction-scoped AsyncConnection, so it cannot truthfully execute concurrent driver operations; multi-connection stress remains an explicit qualification gap.
10. `API_RECOVERY_CONTRACT`: Job lifecycle state, stale-version, validation, and concealed-not-found failures returned safe but unclassified string details, leaving clients unable to distinguish refresh from correction or terminal recovery. The boundary now uses the standard safe failure envelope: stale/state conflicts are `RETRY_AFTER_REFRESH`, validation is `USER_CORRECTION_REQUIRED`, and concealed not-found is `TERMINAL_FAILURE`, with correlation identity when available. A deterministic two-transaction activation race proves one authoritative state/version/Event and one typed stale loser. All 190 Jobs tests, focused Ruff, MyPy, and diff checks pass.
11. `IDEMPOTENCY/DURABLE_EVIDENCE`: Dispatch history bound replay only to Company, key, Appointment, and event type. The same key and operation with a different technician, reason, crew member, version, exception, arrival state, or reconciliation choice could therefore be accepted as replay. New Dispatch command evidence stores a canonical SHA-256 request digest protected by a database length constraint, and every replay compares it before returning authority. Legacy rows without sufficient request evidence fail closed. Migration `d2b1f49e7c0a` passed downgrade/re-upgrade, current-head, and drift checks; all 61 Scheduling/Dispatch tests plus focused Ruff and MyPy pass.
12. `API_RECOVERY_CONTRACT/SAFE_OUTPUT`: Scheduling and Dispatch translated controlled domain errors into bare string details without the standard recovery vocabulary; Dispatch also reflected internal domain exception messages directly. Both boundaries now return correlation-capable `SafeFailure` envelopes. Stale version, capacity, and state conflicts use `RETRY_AFTER_REFRESH`; validation uses `USER_CORRECTION_REQUIRED`; concealed not-found uses `TERMINAL_FAILURE`. Tests assert safe codes and ensure Dispatch internal details are not reflected. All 67 Scheduling/Dispatch tests plus focused Ruff, MyPy, and diff checks pass.
13. `CONCURRENCY/ECONOMIC_IDEMPOTENCY`: Payment collection locked a matching intent row, which cannot serialize concurrent creators when no row exists. Refund and deposit checked replay before later receipt locks without rechecking, allowing concurrent exact commands to race into unique constraints. Webhook, settlement, and posting receipt creators had the same absent-row pattern. A shared Company+operation+identity PostgreSQL transaction advisory lock now protects these creation boundaries before replay lookup. A new independent-engine test proves concurrent collection/refund replay creates one intent, one receipt, one refund, one balance mutation, and one intent Event, while contradictory replay fails closed. All 18 Payments/Invoicing tests plus focused Ruff, MyPy, and diff checks pass.
14. `PROVIDER_UNCERTAINTY`: Synthetic possible-acceptance ambiguity is qualified explicitly. An ambiguous collection persists the intent as `reconciliation_required`, creates one reconciliation exception, creates no payment receipt, and exact replay returns that authority without invoking the provider again. Provider call counting also proves concurrent captured replay invokes collection once. All 19 Payments/Invoicing tests pass; no real provider was contacted.
15. `API_RECOVERY_CONTRACT/SAFE_OUTPUT`: The Payments router reflected controlled exception strings directly, including Payment conflicts wrapping downstream Invoice messages, and returned no recovery classification. Payment not-found, conflict, and validation responses now use fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED` respectively. Branch concealment uses the same non-reflective not-found contract. Adversarial canary details are absent from responses; all 22 Payments/Invoicing tests plus focused Ruff, MyPy, and diff checks pass.
16. `TEST_ISOLATION/STALE_FIXTURE`: Protected authority `4c00ac6` introduced independent owner-read acceptance changes. Cold selective Platform runs exposed model-registration dependencies masked by broad-suite import order, and the catalog-sync test still assumed Scheduling manage was assigned to the bootstrap administrator despite the new read-only policy. Bootstrap and Launch Controls tests now import their referenced Customer/Inventory/Scheduling model graphs explicitly, and the sync fixture expects no manage assignment before deleting the catalog permission. Independent cold processes pass: 7 Bootstrap tests and 12 Launch Controls tests. The reconciled Economics persistence boundary retains both current authorized-Branch admission and OM2-C durable Company+Branch validation; 17 focused Economics acceptance tests pass.
17. `ACCOUNTING_CONCURRENCY/SAFE_OUTPUT`: Accounting Core journal creation checked absent client/source replay evidence before later period locking, allowing concurrent creators to race into unique constraints. Reversal creation did not replay its client key and could attempt multiple prepared reversals before one was posted. Ordered Company-scoped advisory identities now serialize client and source authority; reversal returns only matching evidence and rejects contradictions. A new independent-engine persistence suite proves one journal/source, balanced debit/credit truth, one concurrent posting winner and typed stale loser, one posting Event, and one exact-replay reversal. The Accounting API now uses fixed recovery-classified envelopes instead of reflecting exception text. All 43 Accounting/contract tests plus focused Ruff, MyPy, and diff checks pass.
18. `AP_CONCURRENCY/ECONOMIC_IDEMPOTENCY/SAFE_OUTPUT`: AP creation and application boundaries used absent-row replay checks without a shared identity lock. Concurrent exact requests could race into unique constraints, while credit/disbursement source identities lacked safe replay and application keys returned prior authority without rejecting a changed amount or target. Company-scoped advisory identities now serialize Vendor, source, Bill-number, credit, disbursement, application, and posting-receipt authority; replays validate immutable request evidence and contradictions fail as typed AP conflicts. An independent-engine test proves two concurrent 60.00 disbursement applications create one application/subledger fact and reduce both available/open balances exactly once, while changed-amount key reuse fails closed. AP responses now use fixed recovery-classified envelopes without reflected exception text. All 13 AP/procurement-matching tests plus focused Ruff, MyPy, and diff checks pass.
19. `AP_OBJECT_AUTHORIZATION`: AP creation and aging enforced authorized Branch scope, but Bill lifecycle, duplicate override, credit/disbursement application and reversal accepted any valid same-Company resource ID. A Branch-restricted permission holder could therefore mutate a Bill or replay application authority from another Branch. Every public resource mutation now binds the parent Bill/disbursement to the caller's current authorized Branch set before replay or state mutation and conceals unauthorized identities as not found. The adversarial replay test proves an exact valid application key cannot escape its Branch. All 13 AP/procurement-matching tests plus focused Ruff, MyPy, and diff checks pass.
20. `AP_ACCOUNTING_RECEIPT_INTEGRITY`: AP Accounting posting receipt replay compared only status and journal identity. Reuse of the same source Event ID with a different AP source, mapping version, effective date, or failure reason was accepted as replay, weakening source provenance. Replay now binds the complete immutable receipt authority. Independent concurrent exact replay returns one receipt, while changed source identity fails as a typed conflict; all 14 AP/procurement-matching tests pass.
21. `MIGRATION_GRAPH/RELEASE_BLOCKER`: Protected authority `f6cffe4` integrated Supply Chain migrations from `b0ff279c5aeb` alongside OM2-C's Economics/Dispatch lineage from the same parent, producing heads `19i4k89084j8` and `d2b1f49e7c0a`. Repository-standard `alembic upgrade head` failed immediately. Forward-only merge revision `e3c2a71f8d4b` preserves both histories and restores exactly one head. Existing-schema and entirely fresh PostgreSQL upgrades both reach current=head with no drift; all 65 Purchasing/procurement/idempotency tests pass on the reconciled schema.
22. `PROCUREMENT_MATCH_OBJECT_AUTHORIZATION/STALE_APPROVAL_RACE`: Three-way-match replay returned authority before reauthorizing the referenced PO/Bill and compared only their IDs, so a valid key could escape Branch scope or accept changed expected versions. AP approval also read match freshness without locking the PO, allowing a receipt or other PO mutation to commit immediately after validation and leave an approved Bill backed by stale evidence; its active-match lookup did not exclude superseded history. Replay now reauthorizes both parents and binds expected versions. AP approval shares the per-Bill match advisory identity, selects only unsuperseded authority, and locks the PO while recomputing the complete source digest. A deterministic competing-writer test proves freshness waits and then rejects changed PO authority. All 65 Purchasing/Matching/AP tests plus focused Ruff, MyPy, and diff checks pass.
23. `PURCHASING_DOCUMENT_CONCURRENCY`: Newly integrated document custody uniquely constrained Company+entity+content but serialized only by command key. Concurrent registration of the same artifact under different keys could therefore expose a raw uniqueness failure. Content identity now receives its own transaction advisory lock. Exact normalized custody metadata converges on one immutable record while a changed filename, media type, storage reference, source reference, document type, or Branch for the same content fails as a typed conflict; each accepted alternate key receives durable replay evidence without duplicating the registration Event. The focused backend suite passes 49 tests, the Purchasing route passes 4 frontend tests, and changed-file Ruff, Purchasing MyPy, and diff checks are clean. Two unrelated newly integrated BANK test files retain pre-existing Ruff import-order findings and were not mass-formatted.
24. `PURCHASING_API_RECOVERY/SAFE_OUTPUT`: The expanded Purchasing router reflected controlled domain exception strings directly and returned no machine-readable recovery classification. Inventory/provider-adjacent wrapped errors could therefore expose internal details through receipt and return endpoints. Not-found, conflict, validation, and fallback Purchasing failures now use fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, `USER_CORRECTION_REQUIRED`, and `OWNER_ADMIN_ACTION_REQUIRED` respectively. Adversarial canary details are absent from every response class. The focused backend suite passes 50 tests, the Purchasing frontend route passes 4 tests, and the supported TypeScript plus production Vite build passes.
25. `SUPPLY_CHAIN_BUSINESS_IDENTITY_CONCURRENCY`: Requisition number, first policy per Company+Branch+type, and PO number created during requisition conversion were database-unique but serialized only by command key or parent row. Different-key concurrent creators could race into raw uniqueness failures. Each business identity now takes a transaction advisory lock before its absence check; competing authority fails as a typed Purchasing conflict. Independent connections prove exactly one requisition and one policy survive their respective races, and conversion applies the same protection to PO numbers. The focused backend suite passes 51 tests with clean changed-file Ruff, Purchasing MyPy, and diff checks.
26. `REQUISITION_OBJECT_AUTHORIZATION`: Requisition transition replay validated only Company ownership before returning prior submit/approve/reject/convert/cancel authority. A caller restricted to another Branch could retrieve the transitioned requisition using its valid identity and exact command key. Both create and lifecycle replay now reauthorize the current requisition Branch and conceal inaccessible authority as not found. A direct cross-Branch submit replay fails closed; the focused backend suite remains 51/51 with clean changed-file Ruff, Purchasing MyPy, and diff checks.
27. `FRONTEND_FAILURE/ACCESSIBILITY`: Purchasing document custody discarded the promise from an async mutation handler. A rejected registration could surface as an unhandled promise rejection; the passive failure banner was not announced and offered no explicit retry. The component now contains mutation rejection, retains all entered evidence, emits an assertive accessible alert without backend details, and provides a retry using the retained input. Fields clear only after confirmed success. Component plus route tests pass 5/5, focused ESLint passes, and the supported TypeScript plus production Vite build passes.
28. `PROTECTED_STORAGE_REFERENCE_DISCLOSURE`: Purchasing document custody correctly kept storage locators out of Business Events, but the general workspace and registration response serialized the raw `storage_reference` to every Purchasing read/manage caller. No frontend consumer used it, so this unnecessarily exposed internal provider/bucket/path or signed-reference material. Custody persistence and contradiction comparison retain the locator server-side, while the public document item omits it and continues exposing non-sensitive source provenance plus content/evidence digests. The focused backend suite passes 41 tests; frontend component/route tests pass 5/5; focused Ruff, MyPy, ESLint, TypeScript, and production build gates pass.
29. `REQUISITION_NESTED_OBJECT_AUTHORIZATION/DURABLE_INTEGRITY`: Requisition `job_id` was accepted and persisted without resolving the Job, and the table had no foreign key. Arbitrary, cross-Company, or cross-Branch Job identities could therefore become durable demand provenance. Creation now admits only an existing Job matching the requisition Company and Branch. Forward migration `f4d3b82a9e5c` enforces the same composite relationship against Jobs' Company+Branch+ID authority. Purchasing plus the full Jobs suite passes 230 tests; the migration passes upgrade, downgrade/re-upgrade, current=head, single-head, and drift qualification; focused Ruff, MyPy, and diff checks pass.
30. `INVOICING_CONCURRENCY/REPLAY/SAFE_OUTPUT`: Invoice creation checked replay before locking its Estimate but not afterward. Concurrent same-key creation, or different-key creation for one Estimate revision, could race into idempotency or revision uniqueness constraints. Payment receipt registration similarly performed an absent-row lookup without a shared lock. Creation now rechecks after the Estimate lock and converges exact alternate-key authority on one Invoice while rejecting contradictory commercial terms; payment receipts serialize by Company+receipt identity. Receipt and Accounting posting replays bind their complete immutable evidence rather than only digest or journal/status subsets. The Invoicing router now returns fixed correlation-capable recovery envelopes without reflecting controlled exception text. Independent connections prove one payment receipt and contradictory amount/digest rejection. Invoicing/Payments/Accounting composition passes 67 tests with clean focused Ruff, MyPy, and diff gates. Four existing shared-connection fixture rollback warnings remain classified `TEST_ISOLATION`.
31. `PROCUREMENT_MATCHING_API_RECOVERY/FRONTEND_FAILURE`: Procurement Matching reflected controlled exception strings directly and returned no standardized recovery classification. The workbench did not render those strings, but rejected evaluation and exception-resolution promises escaped their event handlers, and provider-like reconciliation or temporary-unavailable classifications had no explicit safe presentation. Matching now returns fixed correlation-capable `SafeFailure` envelopes with `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED`; an adversarial canary proves internal details are absent. The workbench contains rejected mutations while retaining entered evidence and resolution notes, recognizes structured reconciliation and availability states without rendering backend messages, and announces failures assertively. Procurement Matching/AP/Purchasing composition passes 68 tests; the component passes 3 tests; full ESLint, TypeScript, production Vite build, focused Ruff, and diff gates pass.
32. `INVENTORY_API_RECOVERY/FRONTEND_FAILURE`: Inventory not-found, conflict, and validation exceptions were reflected directly from quantity and reservation operations with no standardized recovery classification. Create, transfer, reserve, allocate, and release UI handlers also allowed rejected mutation promises to escape; create forms cleared only after success but offered only one generic passive message. Inventory now returns fixed correlation-capable `SafeFailure` envelopes using `TERMINAL_FAILURE`, `RETRY_AFTER_REFRESH`, and `USER_CORRECTION_REQUIRED`; a secret-bearing canary is absent from every response. The workspace contains all rejected mutations, retains location/transfer/reservation evidence until confirmed success, interprets structured correction, reconciliation, temporary-unavailable, and terminal states without rendering backend messages, and announces the governed failure assertively. All 29 Inventory backend tests and 6 Inventory route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
33. `PRICE_BOOK_API_RECOVERY/FRONTEND_FAILURE`: Price Book not-found, conflict, and validation exceptions reflected internal text at a commercial pricing boundary, while its fallback lacked machine-readable recovery. All seven UI mutation paths discarded rejected promises and exposed no mutation failure state. Price Book now returns fixed correlation-capable `SafeFailure` envelopes for terminal, refresh, correction, and owner-administrator recovery; canary-bearing internal text is absent from all four response classes. One governed UI wrapper contains category, tax, item, version, activation, option-group, and option failures; clears entered commercial evidence only after confirmed success; and assertively announces structured recovery without backend messages. All 17 Price Book backend tests and 6 route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
34. `ESTIMATE_API_RECOVERY/FRONTEND_FAILURE`: Estimate not-found, lifecycle conflict, validation, and fallback exceptions crossed the Customer→Estimate→Job boundary as reflected or unclassified text. The create-proposal handler allowed rejected promises to escape, and decision controls offered only a passive generic failure state. Estimates now return fixed correlation-capable recovery envelopes for terminal, refresh, correction, and owner-administrator action; adversarial internal text is absent from all four classes. Create rejection is contained while preserving immutable-snapshot proposal inputs. Customer transition/acceptance/rejection controls retain entered evidence and assertively distinguish stale authority from correctable evidence without rendering backend messages. All 33 Estimate backend tests and 7 focused route/control tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
35. `FIELD_SERVICE_API_RECOVERY/MOBILE_FAILURE`: Technician Field Service reflected not-found, lifecycle conflict, and validation exception text through itinerary, Job state, work-note, Customer-disposition, non-billable, and invoice-handoff endpoints. The mobile-facing field panel avoided local success assumptions but collapsed all mutation failures into one passive refresh instruction. Field Service now returns fixed correlation-capable terminal, refresh, and correction envelopes with canary-bearing internal text excluded. The technician panel assertively distinguishes stale authority, correctable retained evidence, reconciliation requiring office review, temporary unavailability, and terminal resource loss without rendering backend messages or enabling blind retry. The supported backend-directory run passes all 10 Field Service contract/conformance/migration tests; the technician recovery test, focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates pass. Two initial root-directory migration-test failures were invocation-path `ENVIRONMENT` results and passed unchanged under the repository-supported backend working directory.
36. `COMMUNICATIONS_AUTHORIZATION_PRIVACY/SAFE_OUTPUT`: Communications reflected authorization, source-not-found, replay-conflict, and validation exception text. Authorization details could distinguish Branch denial, consent policy, or source-domain state, while validation could expose recipient/provider-adjacent data. The boundary now emits fixed correlation-capable envelopes: policy/consent denial and concealed source identity are terminal, contradictory replay requires refresh, invalid input requires correction, and unknown controlled failure requires owner-administrator action. A recipient/provider canary is absent from every response class. All 20 Communications consent, Company/Branch scope, source binding, replay, persistence, permission-composition, and API tests pass with clean focused Ruff, MyPy, and diff gates.
37. `BEACON_AUTHORIZATION_COMPOSITION/SAFE_OUTPUT/TEST_ISOLATION`: Beacon lifecycle and workflow endpoints reflected signal, stale-evidence, ownership-conflict, and invalid-owner details. Claim, assign, transfer, and release were also declared as Analytics-read dependencies even though service policy requires `OWN`, `ASSIGN`, or either for release; valid operators without Analytics read were rejected early, while read-only users reached a later mutation denial. Beacon now emits fixed correlation-capable terminal, refresh, and correction envelopes with signal/provider canaries excluded. Routes bind claim to `OWN`, assign/transfer to `ASSIGN`, and release to a new logged any-of permission dependency; Analytics read alone is denied before service mutation. Direct API tests prove the complete read/own/assign matrix. Collection with Platform authorization exposed a mapper registration dependency, repaired by explicitly registering Customer models; a fresh r7 database eliminated unrelated fixed-fixture collisions and upgraded cleanly to `f4d3b82a9e5c`. The combined Beacon/authorization run passes 143 tests with clean changed-production/new-test Ruff, legacy-test import ordering, MyPy, and diff gates. Eight unrelated pre-existing Ruff rules remain in the legacy authorization test body and were not mass-formatted.
38. `PAYROLL_PROTECTED_OUTPUT/FRONTEND_AUTHORIZATION`: The shared Payroll router reflected every protected conflict and raw `ValueError` across pay statements, artifact retrieval, reporting, compliance schemas, and filing packages. Those messages can contain Employee, SSN/routing, policy, or provider-adjacent details. The Payroll Administration route also initiated three protected readiness/report/schema queries before verifying both reporting-read and run-read authority. Payroll now returns fixed correlation-capable forbidden, refresh, correction, and owner-administrator recovery envelopes; a combined SSN/routing/provider canary is absent from every class. Frontend queries are explicitly disabled unless both read permissions are present, while the existing safe generic failure state remains non-reflective. All 103 Payroll tests pass on fresh r7; two route authorization tests, focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates pass.
39. `FINANCIAL_REPORTING_SAFE_OUTPUT/AUDIT_SAFETY/FRONTEND_AUTHORIZATION`: Financial Reporting reflected not-found and request-validation exceptions. More critically, ledger-integrity exception text was persisted verbatim as an Accounting posting-failure `error_code`, allowing SQL, constraint, Payroll, or protected source details to become durable audit evidence. The report UI also initiated a protected report request before checking report-read authority. Responses now use fixed terminal, correction, and reconciliation envelopes. Durable integrity evidence records only `ledger_integrity_failure`, carries safe report metadata and request digest, and propagates the request correlation identity (or a generated safe UUID) without exception text. The frontend query is disabled without permission and retains its non-reflective reconciliation guidance. All 9 Financial Reporting tests and 4 frontend route/API tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
40. `BUSINESS_ECONOMICS_SAFE_OUTPUT/STALE_FRONTEND_AUTHORIZATION`: Economics workspace validation and result lookup reflected domain details that can include Customer, Job, Payroll, or accepted-source evidence. Workspace query gating had been repaired previously, but once a result was selected, revoking Economics read authority left the nested detail query enabled. Both backend paths now emit fixed correlation-capable correction or terminal envelopes with protected canaries excluded. The result hook accepts explicit authority and disables immediately when permission is absent or stale, matching the workspace boundary. All 151 Business Economics tests and 3 route tests pass with clean focused Ruff, MyPy, ESLint, TypeScript, production Vite build, and diff gates.
41. `PLATFORM_IDENTITY_ADMIN_SAFE_OUTPUT`: Password-reset confirmation reflected the configured password-policy exception, and Company administration reflected not-found, conflict, and validation details that can reveal permission, role, membership, or identity state. Both boundaries now emit fixed correlation-capable recovery envelopes: password-policy and administrative validation require user correction, administrative conflicts require refresh, and concealed administrative absence is terminal. Adversarial policy and administration canaries are absent from every response. A fresh r8 database upgraded from zero to `f4d3b82a9e5c`; the non-Redis Platform identity/administration run passes 30 tests with one Redis-dependent authentication test explicitly deselected, and focused Ruff, MyPy, and diff gates pass. The excluded test remains `ENVIRONMENT`: the unavailable configured `redis` hostname causes the rate limiter to fail closed rather than weakening authentication.
42. `MOBILE_ENGINEERING_SAFE_OUTPUT/PROVIDER_UNCERTAINTY`: The mobile-facing Engineering API reflected internal milestone, external-adoption, notification, realtime-resume, and workstream exception text. Provider repository readiness also returned its internal error as a message without the standard recovery vocabulary, inviting undifferentiated retry of a reconciliation condition. These boundaries now use fixed correlation-capable terminal and refresh envelopes, while repository-readiness uncertainty is explicitly `RECONCILIATION_REQUIRED`. A fresh r9 database upgraded from zero to `f4d3b82a9e5c`; all 31 mobile Engineering API tests pass, including safe recovery classification and cross-Company concealment, with clean focused Ruff, MyPy, and diff gates.
43. `PRIVILEGED_ENGINEERING_SAFE_OUTPUT/RECONCILIATION`: Repository authorization, bounded repository operation, and immutable owner-review APIs reflected conflict, evidence mismatch, eligibility, Git/provider-adjacent, and validation exception text at privileged approval and execution boundaries. They now emit fixed correlation-capable terminal, refresh, correction, or reconciliation envelopes without internal evidence. Repository-operation reconciliation is deliberately separated from ordinary conflict so clients do not retry uncertain repository mutation blindly. A canary covers every translated class. A fresh r10 database upgraded from zero to `f4d3b82a9e5c`; all 22 authorization/operation/review tests pass with clean focused Ruff, MyPy, and diff gates.
44. `TIMEKEEPING_PROTECTED_OUTPUT/CONTROLLED_EXECUTION_SAFE_OUTPUT`: Workday Time reflected authorization, lifecycle, idempotency, and validation exception text across employee punches, corrections, approvals, and Payroll input preparation. Controlled expired-result adoption likewise reflected provider/repository exception text. Timekeeping failures now use fixed correlation-capable terminal, refresh, and correction envelopes; controlled adoption uses a non-reflective refresh contract. Payroll/SSN/routing and provider/path canaries are absent from responses. A fresh r11 database upgraded from zero to `f4d3b82a9e5c`; all 23 Timekeeping and controlled-execution tests pass with clean focused Ruff, MyPy, and diff gates.
45. `MIGRATION_DURABLE_EXCEPTION_PRIVACY`: Financial operational migration intentionally hashes source identities before persisting exception evidence, but then stored raw `ParentResolutionError`, `FinancialValidationError`, or `MigrationRecordError` text beside the hash. Those messages can reconstruct protected source identifiers, financial references, or adapter details and defeat the hashing boundary. Estimate, Invoice, and Payment migration now persist fixed reason-specific detail while retaining machine-readable disposition/reason and the source digest. Tests assert missing-parent and validation detail is fixed and excludes synthetic source/reference evidence. A fresh r13 database upgraded from zero to `f4d3b82a9e5c`; all 5 operational-migration tests pass with clean focused Ruff, MyPy, and diff gates. An r12 rerun after a prior partial execution reproduced known non-cleaning fixture collisions and is classified `TEST_ISOLATION`, not product failure.
46. `CONTROLLED_PROVIDER_SAFE_OUTPUT`: The isolated controlled-execution provider reflected authority-registry and workspace-preparation exception text. Those exceptions can contain execution/lease identities, repository names, branches, filesystem paths, or provider configuration. Execute and authority-refresh conflicts now return a fixed refresh-classified envelope; workspace preparation rejection returns fixed owner-administrator guidance. Authentication remains fixed and fail-closed. All 27 controlled-provider tests pass, the provider boundary contains no remaining direct exception reflection, and focused Ruff, MyPy, and diff gates pass.
47. `ENGINEERING_CAPACITY_SAFE_OUTPUT/RECOVERY_SEMANTICS`: Engineering Capacity serialized every domain exception, including worker, credential, allocation, reservation, and provider state. It also exposed unavailable and reconciliation-required states through the same unclassified 409 shape as ordinary conflict. The boundary now emits fixed correlation-capable terminal, refresh, temporary-unavailable, reconciliation, and correction envelopes according to the concrete failure class; a provider/path canary is absent throughout. A fresh r14 database upgraded from zero to `f4d3b82a9e5c`; all 10 capacity API/service tests pass with clean focused Ruff, MyPy, and diff gates.
48. `WORKER_RECOVERY_SAFE_OUTPUT/NO_BLIND_RETRY`: Owner and authenticated-worker recovery acknowledgement endpoints reflected errors containing worker/execution/offer/lease lineage, journal reconciliation reasons, or local archive state. Both endpoints now return one fixed correlation-capable `RECONCILIATION_REQUIRED` envelope, preserving the deliberate no-blind-retry contract without disclosing internal evidence. The service's durable reconciliation and idempotency behavior is unchanged. All 7 recovery-acknowledgement tests pass with a worker credential/provider-journal canary excluded, and focused Ruff, MyPy, and diff gates pass.
49. `AUTHORITY_RECONCILIATION/AP_FRONTEND_AUTHORIZATION_COMPOSITION`: Protected authority `f0fb25e` correctly admitted Payroll reporting-read and AP report-read users, but the reconciled AP route admitted either operational read or report read while unconditionally requesting both the report-protected aging endpoint and the operational-read procurement-match queue. Thus each single-purpose reader triggered a forbidden cross-permission request. Aging is now enabled and rendered only for report-read authority; procurement matching is mounted only for AP operational-read authority. Direct tests prove report-only and operational-only partial workspaces issue only their authorized queries. Nine focused frontend tests, ESLint, TypeScript, production Vite build, and diff gates pass after protected-authority reconciliation.
50. `STALE_FRONTEND_AUTHORIZATION/OBJECT_READ`: Estimates, Invoices, and Payments list routes rendered a forbidden state without read permission but still initiated their protected list queries before returning. Invoice and Payment direct-detail routes did not gate valid object identities by read permission at all, so a stale or direct URL caused an unauthorized API attempt even though the backend remained authoritative. List and object hooks now accept the current read authority and disable immediately when absent; detail routes render an explicit forbidden state before loading. Tests prove no list/detail API function is called without its exact permission. Twelve focused route tests, ESLint, TypeScript, production Vite build, and diff gates pass.
51. `JOB_FRONTEND_AUTHORIZATION/SOD`: Jobs list and detail routes had no component-level read gate, the list exposed creation to read-only users, and every read-only Job detail rendered lifecycle mutation controls. Backend permissions remained authoritative, but stale/direct navigation caused forbidden reads and presented `JOB_MANAGE` and `JOB_EXECUTE` actions to `JOB_READ` users. Job queries now disable without read authority, creation requires manage authority, and lifecycle controls require execute authority. Tests prove read-only inspection remains available without mutation controls and direct unauthorized object reads are not requested. Ten focused Job route/lifecycle tests, ESLint, TypeScript, production Vite build, and diff gates pass.

## Release-readiness projection

- Release SHA: starting authority above plus the OM2-C qualification commit recorded on this branch.
- Schema head: candidate `f4d3b82a9e5c` over merged `e3c2a71f8d4b`; fresh-upgrade ancestry, focused downgrade/re-upgrades, and drift clean.
- Backend: broad functional coverage is high, but the integrated Redis test and a clean one-pass fresh-database rerun remain external gates.
- Frontend: tests/lint/build qualified again after the bounded dependency repair.
- Security: focused authorization/isolation/idempotency/safe-output suite qualified; dependency transitive findings and expanded direct API object-binding remain open work.
- Known P0/P1 defects discovered in this tranche: none.
- Projection: **NON_PRODUCTION_CONDITIONALLY_QUALIFIED**. Enterprise retains deployment and release authority.

## Reproduction methodology

Host pressure was checked before heavyweight work. Tests used Python 3.12 and an isolated local PostgreSQL 16 database created only for OM2-C. The standard container hostname `postgres` was replaced through `DATABASE_URL`; no shared lane database was modified. Broad backend invocation required `ENVIRONMENT=test PYTHONPATH=.`. No Production load, provider calls, real journals, real payments, or real data were used.
